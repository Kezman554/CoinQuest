Convention: a session letter in this log comes from the board card that
commissioned the work, never inferred from the previous entry. The prompt
that starts a session does not state its own letter, so guessing "the next
one in sequence" breaks the moment a card is appended out of alphabetical
order — which happened once, corrected 2026-08-25 (see that date's entries
and their note below). An unplanned prompt — a fix, a polish pass, a
follow-on arising from real use rather than a card of its own — carries no
letter at all: a date, a title, and a line saying what it followed on from.

2026-08-20 — Session A: project scaffold

Python side:
  - app/ package with routers/, models/, services/, migrations/ (all empty
    so far), tests/, requirements.txt pinned, pytest.ini.
  - app/config.py reads CHILD_NAME, PARENT_PIN, DATABASE_URL, COINQUEST_PORT
    and TZ. The first two have no default and raise ConfigError if absent.
    A variable set to empty counts as absent — the checked-in .env leaves the
    optional three blank, which must not crash the service.
    TZ is resolved to a ZoneInfo at startup, so a bad zone fails immediately
    rather than part-way through a settlement.
  - app/main.py: FastAPI app with GET /health, returning status, timezone and
    the current London time.
  - tzdata is a dependency. Windows carries no IANA database and a slim
    container image may not either; Europe/London must resolve everywhere.

Frontend:
  - frontend/ scaffolded with Vite + React + TypeScript.
  - Dev server proxies /health and /api to the API on 8600, so every fetch in
    the app is a relative path in development and in the container alike.
  - App.tsx calls /health and shows the result — proof the wiring works.

Verified: pytest 7 passed; uvicorn serving /health 200 with +01:00 (BST
correct); npm run dev serving the app and proxying /health 200; npm run build
clean including the tsc typecheck.

Not done yet: no database, models or migrations (Alembic is installed but not
initialised), no Dockerfile, and the API does not yet serve the built bundle.

2026-08-20 — Session B: money and calendar services

app/services/money.py
  - Integer pence throughout. format_pence(450) -> "£4.50"; parse_pence
    accepts "4.50", "£4.50", "4", ".5", "1,234.56", "450p" and signs.
  - Parsing rejects a third decimal rather than rounding it: an amount that
    cannot be paid is a typing mistake, not a value to guess at. A float
    passed as an amount raises MoneyError rather than being coerced.

app/services/calendar.py
  - Every function takes the timezone as an argument. Nothing reads the
    system zone and nothing defaults to one; the container clock is UTC.
  - Period(start, end, tz) holds whole local days. start/end are inclusive
    as a person would say them; starts_at/ends_before are the instants, and
    are half-open so 23:59:59.7 cannot fall out of the week that owns it.
  - week_start, week_containing, week_containing_instant, current_week,
    day_containing, month_containing, month_bounds, local_date, today,
    start_of_day, elapsed.
  - The week runs Sunday to Saturday. Both British clock changes fall on a
    Sunday, so a London week boundary at midnight is never ambiguous.

One trap found and defended against: when two aware datetimes share a tzinfo
object, Python subtracts them by wall clock and ignores the zone, so the
October week measured 168 hours instead of 169. elapsed() and
Period.duration go via UTC, and a test asserts both the wrong answer and the
right one so the reason cannot be lost.

Tests: 93 pass. Covers the BST-to-GMT week (169h), the GMT-to-BST week
(167h), the 25- and 23-hour days, both halves of the repeated 01:30, a week
spanning a month end and a year end, month bounds opening in BST and closing
in GMT, and pence round trips over every penny to £2. The suite passes with
the system zone set to America/Los_Angeles.

No float appears in either module or their tests, other than the two passed
deliberately to format_pence and parse_pence to assert they are rejected.

2026-08-20 — Session C: schema and the first migration

Tables (app/models/): chore_definitions, chore_instances, weeks,
settlement_lines, waivers, earnings_ledger, savings_ledger.

The separation the scheme turns on:
  - A definition is a rule and will be edited. An instance is working data
    and may point at one freely.
  - A settled week keeps its own figures, and settlement_lines keep their own
    copy of the chore's name, category, cadence and amount as they stood at
    the moment of settlement. source_definition_id is provenance only,
    nullable, ON DELETE SET NULL, and never read to work out money.

Enforced by the database, not by the application remembering:
  - Triggers refuse any change to a closed week's figures or status, any
    update or delete on either ledger, and any edit to a settlement line.
    paid_at and deposited_pence stay writable, because payday follows
    settlement.
  - CHECKs: a week starts on a Sunday (strftime %w) and runs seven days; a
    closed week carries all four figures and they add up; a voided week pays
    zero; no amount anywhere is negative; only a withdrawal is negative in
    savings and the balance never goes below zero; a weekly_count chore
    states its count and no other cadence may; a waiver fills in the columns
    its scope allows; a reward states its reason.
  - Partial unique indexes: one instance per chore per day, or per chore per
    week for the week-scoped cadences.

Alembic initialised at app/migrations with the URL supplied from the
environment through app.config, never from alembic.ini. render_as_batch is on
because SQLite cannot alter a column. alembic check reports no drift, and
downgrade base leaves an empty file.

One conflict found and resolved: ON DELETE SET NULL on a settlement line's
provenance column fires an UPDATE, which the blanket append-only trigger
blocked — making a definition undeletable once settled, and the SET NULL dead
configuration. The trigger now freezes every column that carries the money or
the story and permits exactly one change: provenance being cleared, never
repointed.

Instants are stored UTC and returned aware via a UtcDateTime type; a naive
datetime is refused rather than assumed. Local days are separate Date columns,
computed in Europe/London by app.services.calendar.

Tests: 138 pass. Schema tests build the database by running the real migration
against an empty file, not create_all, because the triggers exist only in the
migration. The central case asserts that renaming, repricing, recategorising
and withdrawing a chore leaves a settled week's stored figures untouched, and
that deleting the definition outright does too.

2026-08-20 — Session D: generating a week's assessable instances

app/services/instances.py. Pure: it reads no clock, writes nothing, and takes
a Period, the definitions and the waivers.

  - DAILY produces one instance per day the week contains — seven, including
    in the 169-hour October week.
  - WEEKLY_COUNT produces n instances tied to no day, numbered 1..n so each
    is separately claimable.
  - WEEKLY_CONDITION produces no instance at all. It appears in the plan as a
    deferred judgement, made once at settlement: a condition held all week
    cannot be assessed before the week has finished happening.
  - ONE_OFF and EVENT are not derived from a week. Both are created by a
    parent when they happen and neither is predictable from a definition.

Waivers: a waived day removes that day's daily instances; a chore waived for
the week is removed whatever its cadence; a day waiver outside the week or a
chore waiver for another week is ignored. A week-long condition is not made
of days, so waiving one does not reduce it.

The scaling table is data, in WEEKLY_COUNT_BANDS: (up_to_days_waived,
reduce_by, waives_entirely). 0-2 full count, 3-4 one occasion off, 5-7 not
assessed. Every function takes the bands as an argument, and a test replaces
the whole table with a harsher one and asserts the logic is untouched. Days
outside the table raise rather than being guessed at.

Schema change this card forced (revision b743007f1216): the first migration
allowed one week-scoped instance per chore per week, which is right for a
condition and wrong for a chore due three times a week. Added a `sequence`
column and moved the partial unique index to
(definition_id, week_id, sequence). Verified the batch rebuild left all eight
triggers in place.

sync_week_instances writes a plan to the database. It never touches an
instance the plan still wants, and removes one it no longer wants only if it
is still UNTOUCHED — waiving Monday after the child's claim was confirmed
leaves the claim standing for a parent to decide about.

Tests: 171 pass. The three cases the card asks for — 0, 3 and 6 waived days —
assert 7/2/3, 4/1/2 and 1/0/0 respectively.

2026-08-20 — Session E: claiming, and a parent ruling on claims

app/services/authorisation.py — verify_pin compares the offered PIN against
the configured one with hmac.compare_digest, and returns an Authorisation
naming the party. The party is derived from the credential, never taken from
the request: a client says what it wants done, never who it is. When a second
parent is authorised, this is the one place that changes.

app/routers/dependencies.py — a session per request, committed by the request
itself rather than by the dependency, which is what makes a batch atomic. The
PIN field is a pydantic SecretStr so it cannot be echoed in a repr, a log line
or a validation error.

app/routers/claims.py
  - POST /api/claims — the child, no credential. A claim is a request to be
    believed, not an assertion that money is owed. Refused if the instance is
    already claimed or the week is closed.
  - POST /api/claims/review — a parent's batch, one PIN for the submission.
    The PIN is checked before anything is read; every decision is applied to
    the session and nothing is committed until all of them have applied. Any
    failure rolls the whole thing back and names the item that stopped it.
  - POST /api/instances/{id}/missed — a parent marking a miss without waiting
    for settlement, which is what makes the recovery window usable. Same
    authorisation. Confirmed work is not taken back this way.

A rejected claim returns to UNTOUCHED, not MISSED. Not believing a claim is
not the same as ruling the chore was not done, and an untouched instance stays
provisional until settlement, so the child can still do it.

Schema (revision e42da40283c5): chore_instances.authorised_by, with a CHECK
that a confirmed instance names its author. Alembic does not autogenerate
CHECK constraints on SQLite, so that was written by hand. Two older tests
confirmed work without an author and now say who agreed it.

Verified against a live server, not only the test client: a claim with no
credential returns 200, the same batch with a wrong PIN returns 401 and with
no PIN field 422, and with the correct PIN returns 200 and stores
authorised_by='parent'.

Tests: 196 pass. The three the card asks for — wrong PIN, a batch containing
one inapplicable item, and a valid batch — assert 401 with nothing moved, 409
with nothing moved, and confirmations and rejections applied together.

Not done: there is no attempt limiting on the PIN. A four-digit PIN on a
household LAN can be brute-forced by anything that can reach the port.

2026-08-20 — Session E addendum: rejections leave a trace

A rejected claim returned the instance to UNTOUCHED and cleared claimed_at,
which is the right state, but nothing survived to say the rejection had
happened. From the child's side that is indistinguishable from a tap that
never registered, so he re-claims into the same refusal; and three rejections
left a row reading exactly like one nobody had ever touched.

Revision 7a0c95a307e0 adds chore_instances.rejected_at and rejection_count,
with CHECKs that the count is not negative and that a count above zero has a
time to go with it. Neither column changes a rule — the state is still
UNTOUCHED and still provisional — so the week view can later say "not
accepted, have another go" instead of silently losing the claim.

Claiming again deliberately does not clear either column: claiming does not
unhappen a rejection. A confirmation after a rejection keeps both facts.

Tests: 203 pass, including three rejections on one instance, re-claiming
after a rejection, and a refused batch recording no rejection at all.

2026-08-20 — Session F: misses, recovery, and the best assignment

app/services/recovery.py. Pure apart from one writer.

Assessment is against the requirement, never a count of rows. Requirement
comes from the week's plan; the question is whether that many were confirmed,
and shortfall is floored at zero. A waiver applied after work was confirmed
leaves a confirmed instance the plan no longer asks for, and that surplus is
harmless: it cannot go negative and cannot buy off a miss elsewhere. Both are
tested.

Misses: a parent-marked miss is definite and fills the shortfall first; the
rest are inferred at settlement. If a parent marked more missed than the week
ended up short of — which a later waiver can cause — the extra ones simply do
not become misses.

An inferred miss has no author. record_inferred_misses writes
miss_origin=inferred_at_settlement and clears authorised_by, because if the
child had a claim rejected on that instance earlier in the week the rejecting
parent's name is still sitting there and would read as though they had ruled
it missed. The rejection survives in rejected_at/rejection_count, which is
where that fact belongs. Revision 82826e03b64d adds miss_origin with three
CHECKs: a miss says how it arose, an inferred miss has no author, and a
parent-marked one names the parent.

Recovery eligibility is derived from ON_DEMAND_CADENCES in enums.py — daily,
weekly_count, one_off. A weekly condition cannot be started on Thursday and an
event is not the child's to decide. No chore name appears in the rule.

The optimiser evaluates every lawful assignment (subsets of completed,
spendable bonus chores up to the cap) and takes the highest total, with
deterministic tie-breaking because the figure is about to be stored forever.
Two things the obvious answer gets wrong, both tested: spending a 300p bonus
when a 100p one would do, and spending anything at all when the misses exceed
the cap.

Discovered while testing: recovering is not always worth doing. Two misses
rescued with 400p of bonus chores to save 350p of chore pay is a 50p loss, and
the optimiser correctly declines. Two of my own tests asserted otherwise and
were wrong.

Also fixed here: one_off and event chores have no plan-derived requirement, so
a completed one-off bonus was invisible to the assessment. For those two
cadences the instances a parent created are the requirement
(WEEK_DERIVED_CADENCES marks the difference).

Appendment: a weekly condition ignores the reduction band — there is no
fraction of "kept tidy all week" to take off — but honours the waives-entirely
band at the same threshold as the counts.

Tests: 236 pass, including the card's three weeks (more bonuses than misses,
more misses than the cap, and greedy paying less than best) and a permutation
test asserting chore ordering never changes the figure.

Open question for settlement: the chore pay is treated as one pot, paid in
full only if every basic requirement was met or recovered, on the strength of
"exceeding the cap fails the chore pay".

2026-08-20 — Session G: proposing and closing a week

app/services/settlement.py

  propose() computes what an open week is worth and writes nothing. It can be
  called by a dashboard tile every five minutes and change nothing; a test
  snapshots the whole database around three calls and asserts equality. In
  particular untouched instances stay untouched — they become misses at
  settlement and not before.

  settle() takes the figure the parent actually read, as agreed_total_pence,
  and refuses if the week no longer proposes it. Storing forever an amount
  nobody agreed to is the one mistake that cannot be corrected afterwards.
  It records the inferred misses, links each spent bonus instance to the miss
  it covered via recovered_instance_id, writes a SettlementLine per assessed
  chore, writes the week's four figures, and writes one earnings entry.

  void() closes a week at zero, keeps every instance exactly as it is, and
  requires a reason.

  propose() refuses a week that is not open. That refusal is the mechanism
  behind "no code path recomputes a settled period", not merely the intention:
  there is no route from a closed week back to a current definition.

app/routers/weeks.py — GET /api/weeks, GET /api/weeks/{id} (stored figures if
closed, proposal if open), GET /api/weeks/{id}/proposal, and POST .../settle
and .../void. Reading needs no PIN; closing always does.

Two interpretations to check:

  "Base" is read as the chore pay the week put at stake — everything the basic
  chores were worth — with chore_pay_pence being what it actually came to,
  the whole of it or nothing. If the scheme means a separate unconditional
  weekly allowance, that is a new figure and is not modelled.

  Voiding "does not touch rewards already earned": the week's own figures must
  all be zero (the schema requires it), so the rewards confirmed that week are
  written to the earnings ledger as REWARD entries in their own right. The
  week pays nothing and the reward is still his.

Tests: 261 pass. Settling a week, changing the chore's name, amount and
availability, and reading the week back gives the same figures and the old
names. Deleting every definition outright gives the same figures too.

Verified live: a proposal without a PIN, a settle refused for disagreeing with
the figure, a settle agreed, then repricing the chore from 200p to 900p and
reading 200p back, and the proposal endpoint refusing a settled week.

2026-08-20 — Session G addendum: the base allowance, and voided rewards

Two corrections, both from the scheme document rather than from the code.

§1 pays a base allowance every week regardless of how the chores went. That
had been collapsed into what I was calling "base", which was actually the
§2 chore pay at stake. Revision 4e720209aaba renames settled_basic_pence to
settled_chore_pay_pence and adds settled_base_pence beside it; a week's total
is now base + chore pay + bonuses + rewards. WEEKLY_BASE_PENCE is
configuration, defaulting to 100. The proposal now carries base_pence,
chore_pay_at_stake_pence and chore_pay_pence as three distinct figures.

§11: a voided week loses the base, the chore pay and the bonuses — the three
things it names — and rewards keep settling through the ordinary path. The
schema demanded a voided total of zero, which had forced void() to write
rewards to the earnings ledger as a special case. That was a double-payment
waiting to happen: a void is liftable until the week settles, and a reward
written at void-time would be paid again at settlement with an append-only
ledger unable to take it back. The constraint now zeroes the three components
instead, and void() writes reward settlement lines the same way settle() does.

Two things worth knowing for the next migration touching `weeks`:

  - A SQLite batch rebuild recreates the table and silently drops any trigger
    on it. This revision drops and recreates both weeks triggers explicitly,
    and they name the renamed column anyway.
  - An empty batch_alter_table block does not rebuild even with
    recreate="always"; it needs an operation in it. There is a deliberate
    no-op alter_column doing that job, with a comment saying so.

Also: Bash's /tmp and Python's /tmp are different directories on Windows, so a
manual database reset from the shell was silently doing nothing. Not a code
problem, but it wasted a cycle.

Tests: 268 pass.

2026-08-20 — Session H: payday, savings and parent-entered rewards

app/services/payments.py
  - Settling and paying are separate acts. A settled week reads as owed until
    somebody hands the money over, which is a state the app has to show:
    otherwise "did we actually pay him for that week?" is answerable only
    from memory, which is the problem this app exists to solve.
  - One payment clears several weeks. The deposit is apportioned across them
    in date order so each week records its share and each savings entry says
    which week it came from — bookkeeping, not a decision: the child chose one
    number and it is the total.
  - What he keeps is reported and stored nowhere. Cash in a pocket is not this
    app's business and a figure for it would be wrong within a day.
  - A week owes its settled total plus any parent-entered reward belonging to
    it. Nothing is applied unless every week in the payment can be.

app/services/savings.py — opening balance (once, and it must be first, because
everything after it is a movement from a balance), deposits, withdrawals, and
the running balance. Each row carries the balance after it, written once into a
row that cannot be edited.

app/routers/rewards.py — POST /api/rewards and POST /api/rewards/presets/{key},
plus GET /api/rewards/presets. A reward carries an amount and a reason, belongs
to the week it was entered in, and never touches that week's settled figures: a
bad week at the hoover does not make an award smaller, and a void does not take
it back. Amounts may be given as pence or typed as pounds, which is the first
use of app.services.money at an actual edge.

The school award preset is data. Its amount is a default rather than a price —
the preset saves retyping the name and reason, not deciding what something is
worth this time.

Revision 9040ce063e24 relaxes the deposit constraint: a deposit is no longer
capped at one week's settled total, because a week can owe more than it settled
for once a reward belongs to it. The cap that matters is against what was
actually handed over, and only the payment knows that. Weeks triggers dropped
and recreated around the rebuild again.

Tests: 300 pass. Verified live: two weeks settled at 450p each, both paid in
one action with 600p banked, apportioned 450 + 150 across the two weeks with
running balances of 450 and 600, and both weeks stop reading as owed.

Numbers to confirm: WEEKLY_BASE_PENCE defaults to 100 and the school award
preset defaults to 100. Both are in one place each.

2026-08-20 — Session H addendum: the Eagle award, and rewards after settlement

The preset is the Eagle award: £3 every time the school gives him one, no
limit (§7). Fixed name as well as fixed amount — the name is what he calls it
and what makes the ledger readable a year later, so it is the reason recorded
against the entry. Sending a different amount to the preset endpoint is
refused with 422 rather than ignored; quietly substituting a number for the
one asked for is worse than saying no. /api/rewards remains for a reward of
any amount.

The edge: a reward entered for a week that has already settled. A reward now
always lands on an open week — its own if open, otherwise the next open one,
otherwise a week opened after the latest on record, so he gets it with the
next lot of money. occurred_on still records when it actually happened, and
the response says carried_to_an_open_week so nobody has to work out where it
went.

The alternative — recording it against no week at all — was rejected for a
concrete reason rather than a stylistic one. Payment tracking lives on weeks
(paid_at, deposited_pence), and the earnings ledger is append-only, so there
is no paid_at to write on a ledger row and no way to mark an unattached
reward as handed over. It would have been owed forever. Everything that is
owed has to land on something that can be paid, and a week is the only such
thing in this schema.

Tests: 306 pass, including a reward for a settled week, for a voided week,
with no open week available, three rewards not creating duplicate weeks, and
a carried reward still being owed and paid.

2026-08-20 — Session P: attempt limiting on the PIN

app/services/lockout.py. In-memory, per source address, cooling off on its
own. PIN_ATTEMPT_LIMIT (5) and PIN_COOL_OFF_SECONDS (300) are configuration.

The trade-off, decided rather than avoided: counting per source means a device
that guesses wrong locks only itself, so nobody can lock the parent out of
their own app by guessing wrong on purpose — which a single global counter
would allow, with no administrator to appeal to. What it costs is that an
attacker with several devices, or one that can change its address, gets a
fresh budget for each. That turns seconds into a long evening rather than
making guessing impossible, which for this app is the right end of the trade.

The address comes from the socket. Nothing the caller says about itself is
read — a limiter keyed on a claim limits nobody. Behind a reverse proxy every
request would arrive from the proxy, so this degrades to a global counter with
the lockout risk that implies; that is the point at which somebody has to
decide which proxy header to trust. Written up in the module docstring.

The lockout is checked before the PIN is looked at, so a correct guess cannot
open the window early. A wrong PIN is still told nothing; a lockout is told
plainly how long remains, because the likeliest reader is a parent who
mistyped. Retry-After carries the same number.

Found while testing: alembic's fileConfig disables every existing logger by
default, so running a migration in-process silenced coinquest.authorisation —
the logger whose entire job is recording failed attempts. env.py now passes
disable_existing_loggers=False.

Also: the limiter is process-global by design, so tests reset it between
cases via an autouse fixture. Without that, one test's wrong PIN locked out
every test after it.

Tests: 331 pass, with a fake clock so the suite never waits. Verified live
against a running server: four wrong PINs, then 429 with "Try again in 10
seconds", a correct PIN refused while cooling off, the stated wait counting
down, and the correct PIN accepted at ten seconds without a restart. The log
shows each failure and the lockout.

2026-08-20 — Session P revision: the cooling-off escalates

Per-source counting stays and the limit stays at 5. The cooling-off now starts
at 30 seconds, doubles on each consecutive lockout from the same source, caps
at 900, and resets to the start on a correct PIN.
PIN_COOL_OFF_START_SECONDS and PIN_COOL_OFF_MAX_SECONDS are configuration.

The reason, which is not the one I had written down: the kitchen wall screen is
a single source used by the child and the parents both — it is where he claims
and where they confirm — so on the surface that matters most, "a guesser locks
only itself" means he locks his parents out of their own app. A flat five
minutes made the ordinary case (a mistype, a bored nine-year-old) expensive and
the hostile case cheap: 5 guesses per 300s is about 1,440 a day from one
address, the whole four-digit space inside a week. Escalating inverts that. A
mistype costs half a minute; sustained guessing reaches the 15-minute ceiling
after six lockouts and needs over 20 days for the keyspace. Both numbers are
asserted in a test rather than left as a claim in a comment.

Waiting a cooling-off out clears the failure count but not the lockout count,
so the doubling cannot be reset by simply serving each one. Only a correct PIN
clears it.

Tests: 341 pass. Verified live with a 4-second start and a 16-second cap:
4 -> 8 -> 16 -> 16, then a correct PIN, then back to 4. The log names the
lockout number each time.

2026-08-20 — Session Q: settling on an assignment a parent supplied

app/services/recovery.py gains assignment_from(assessment, supplied) and
SuppliedRecovery(spend_definition_id, for_definition_id). A supplied
assignment is scored by the same evaluate() as the computed one, after being
checked against the same week: at most `cap` recoveries; each spent chore must
be a bonus, completed and confirmed this week, of a cadence that can be done
on demand, and spent once; and each miss recovered must have actually happened,
counted per chore. A parent may lose money. They may not claim something untrue
about the week.

app/services/settlement.py: propose(..., override=...) scores the supplied
assignment and carries optimum_total_pence alongside — what the app would have
paid — plus foregone_pence, the difference. settle() writes overridden_by,
override_reason and optimum_total_pence.

Revision 1eb8e8b3e4ae adds those three columns, with CHECKs that an override
belongs to a settled week and names both its author and what it was chosen
over. The immutability trigger now covers them: an override is part of the
closing figures and is frozen with them.

Router: POST /api/weeks/{id}/proposal previews a week under a supplied
assignment, because a parent has to be able to read a figure before agreeing
to it. agreed_total_pence is checked against whichever proposal was made, so
submitting the computed figure alongside an override is refused, and so is the
reverse.

A real bug found and fixed on the way. Two earlier migrations built their
table shape with copy_from=Week.__table__, importing the live model. The
moment this session added three columns, those revisions could no longer run
against an empty database — the rebuild tried to copy columns that do not
exist yet at that point in the history. A fresh deploy on the Pi would have
failed. Each weeks-rebuilding migration now defines the table literally, as of
itself, and the whole chain builds from empty again.

Tests: 367 pass. An override that loses 50p is proposed, settles at 450 rather
than 500, and records both figures. Exceeding the cap, spending a chore that
was never completed, spending one twice, spending a basic, spending a
week-long condition, and recovering a miss that did not happen are each
refused with the rule that was broken. A settled overridden week refuses both
proposal doors, cannot be settled twice, has every override column frozen by
the trigger, and reads the same after the scheme changes underneath it.

2026-08-20 — Phase 2 close: wording, a required reason, and voids

1. Refusals are written for a person. "was missed 1 times this week" becomes
   "was missed once this week", a miss that never happened says so rather than
   reporting zero, and counts read "1 miss" or "2 misses". The lockout message
   had the same fault — "Try again in 1 seconds" — and is fixed with it.

2. An override that settles below optimum_total_pence must carry a non-empty
   reason. Enforced in settle() and by a CHECK (revision 3c1f6a2d9b74), since
   "a settled week explains itself" is exactly the rule a later caller forgets.
   An override that costs nothing needs no reason: there is no difference to
   explain. A blank or whitespace reason counts as none.

   Ordering matters here and was wrong at first: the agreed-figure check now
   runs before the reason check. Somebody agreeing the wrong number has not
   read the right proposal, and asking them to justify a choice they have not
   seen is no help.

3. A void takes no assignment. §11 zeroes the base, the chore pay and the
   bonuses, so there is nothing for a make-good to rescue, and accepting one
   would record a deliberate choice that changed nothing. Sending an override
   to the void endpoint is refused by name rather than ignored in silence, and
   an already-voided week refuses an override through both proposal doors.

The question outstanding from H was answered in f3b346e, not left open: a
reward always lands on an open week — its own if open, otherwise the next open
one, otherwise a week opened after the latest on record. The alternative, a
ledger entry belonging to no week, cannot work: payment tracking lives on
weeks (paid_at, deposited_pence) and the earnings ledger is append-only, so an
unattached reward could never be marked as handed over and would be owed
forever. open_week_for() carries that reasoning, and five tests cover it.

Tests: 375 pass. Verified live: computed 500p, override 450p with 50p
foregone, refused without a reason, void-with-override refused, and settled
with a reason recording both figures.

2026-08-21 — Session I: the week view

The child's screen, and the endpoint it needed. There was no way to read a
week's instances over HTTP at all: /api/weeks/{id} carries the figures and
says nothing about what is on the list.

app/services/week_view.py — pure presentation of facts the scheme services
already worked out. It decides no money: the figures come from
settlement.propose, the requirement from instances.plan_week, and what
happened from the stored instances.

  Two groupings, because they are two different things:
    - A daily chore belongs to a day, and an empty Tuesday is a question
      about Tuesday.
    - A weekly-count chore belongs to the week. Laying "three times before
      Saturday" out as seven boxes with four greyed out invents an absence on
      four days the scheme never asked about.
    - A week-long condition is a third thing again: no occasions, nothing to
      tap, judged once at settlement.

  Every day of the week appears, including waived ones. Dropping a waived day
  makes the week look six days long; showing it empty makes it read as a day
  nothing was done on. It is neither, and it says "Day off" and why.

  An untouched instance on a day already past is still claimable, because it
  is still provisional. "You did not tick Monday" must not read as "Monday is
  lost" while it is not.

app/routers/week_view.py — GET /api/week, GET /api/week/{id} and
POST /api/week/open. None carries a PIN: reading changes nothing, and opening
a week asserts nothing about what was done. Opening is a write on purpose —
a week exists as a row only once something has happened in it, so the first
load of a Sunday would otherwise show an empty screen with no way to fix it.
It is idempotent and the frontend calls it on every load.

The recovery panel counts only misses a parent has actually ruled, and works
out coverage itself rather than reading proposal.recoveries. The difference
is not cosmetic: mid-week the proposal counts every untouched instance as a
prospective miss, so on Monday it exceeds the cap and correctly declines to
recover anything. Right for the money, wrong for the screen — what the child
needs to know is whether the miss he was told about is put right. Settlement
still decides the money, on the week that actually happened, and is not bound
by anything the screen said.

frontend/: api.ts (types and the two calls), words.ts (all the wording, in one
place), components/ and App.tsx.

  - The wording changes with the time left rather than getting louder. "Do
    the car or the path to put it right, you have 3 more days — until
    Saturday" while there is a tomorrow to act in; "Today is the last day to
    fix Make your bed. The week ends tonight" inside the last 24 hours; and
    when no bonus chore is left, it says so and drops the clock entirely
    rather than pointing at one for a race already over.
  - Amounts are written as they are said: 40p, £1, £4.50. Never £0.40.
  - Counts never read "1 times", lists are "A, B and C", options are "or".
  - A refused claim says "Have another go" and "Not accepted 2 times". The
    state is UNTOUCHED, which is right and unreadable on its own.
  - Every tappable thing is a full-width row at least 3.5rem tall, nothing is
    smaller than 1rem, and colour never carries a meaning by itself.

Found while testing, and left alone deliberately: a WEEKLY_CONDITION can
never be confirmed. It produces no instance, so nothing can be claimed and
nothing confirmed, and assess_week counts it required-and-not-confirmed from
the first minute of the week — a standing miss no one can clear. The miss is
inferred rather than ruled, so it appears nowhere in the recovery panel
(correctly: there is nothing the child could do), while the optimiser quietly
spends a completed bonus chore covering it. A week in which everything was
done reads as chore pay in full and the bonus gone; the live drive showed the
panel saying "Put right" while the chore pay sat at zero, because the
condition was the other miss.

That is a hole in settlement, not in the view, and the fix belongs with the
parent view where the judgement would be made — most likely a confirmable
instance created for the condition at settlement, so a parent rules on it the
way they rule on anything else. Recorded in
test_a_week_long_condition_counts_against_the_week_until_it_is_judged so it
cannot be lost. Until then the card says "Kept up all week. Checked on
Saturday", which at least makes the figure explicable.

Tests: 392 + 17 = 409. The card's week — a miss, a recovery still available,
and a waived day — is driven end to end: the miss is ruled, the panel names
it with the deadline, the bonus is worked, the panel reads covered, the rest
of the week is completed and the chore pay comes back at 140p for 100p of
bonus given up.

Verified live against a running server and a seeded week: the week opened
twice with the same instance ids, a claim through the Vite proxy, and the
components rendered against the real payload to read the actual words —
which is how the £0.40, the "0 of 1 done", and the chore name printed twice
in four inches were caught.

Not done: the API still does not serve the built bundle, and there is no
parent view, so confirming a claim needs a request by hand.

2026-08-21 — Session J: the parent view

The queue, what a batch does, the acts, and a closed week that reads as closed.

app/services/review.py — the batch logic, lifted out of the claims router
because two callers need it now and they must not drift apart. The commit
applies it for real; the parent view asks what it *would* do first. A preview
computed by different code from the commit is not a preview, it is a second
opinion, and the moment the two disagree the figure a parent agreed to is not
the figure that happened.

app/services/parent.py — pending(), and preview().

  preview() answers the question a parent actually has in front of a list of
  ticks: what does agreeing to this do? It applies the batch for real inside a
  SAVEPOINT, proposes each affected week from the result, and rolls the
  savepoint back in a finally. Applying it is the only way to be certain the
  preview and the commit agree — they are then the same code over the same
  data — and the rollback is what makes that safe.

  What comes back is per week and in the week's terms: misses before and
  after, which bonus chore is about to be spent covering which miss, whether
  the chore pay is rescued or lost, and the total either side. "7 items" is a
  fact about the screen. None of the above is derivable from it.

  The author recorded during a preview is "preview", not "parent". Nothing
  written under it survives the savepoint, and using the real string would put
  the record one bug away from being unable to tell a rehearsal from the
  thing itself.

app/routers/parent.py — GET /api/parent/queue and POST
/api/parent/review/preview. Neither carries a PIN. The preview writes and
then throws the write away, and what it exposes is what a proposal already
exposes: what a week would be worth. The PIN guards the act, not the
arithmetic. Confirming for real is still POST /api/claims/review, one PIN,
one transaction, all or nothing.

app/routers/waivers.py — new; there was no way to record a waiver over HTTP
at all. POST re-plans the week it lands in through sync_week_instances, which
removes an occasion the plan no longer wants only while it is still
UNTOUCHED. Waiving Monday after the child's claim was confirmed leaves the
claim standing. A waiver is not a way in through the back door to deleting
somebody's confirmed work. Refused for a closed week: a closed week is not
re-planned, its figures are stored.

Savings gained the two endpoints the parent view needed: POST
/api/savings/withdrawals, and POST /api/savings/reconcile.

  Reconcile records nothing, and that is the design rather than an omission.
  The ledger is append-only with four entry types, none of which means "the
  books were wrong". A fifth would make the balance agree while destroying
  the only information the difference carries — that something happened to
  the account nobody wrote down — and the monthly match is computed from the
  lowest balance a month reached, so an invented adjustment silently moves
  money the scheme is about to pay a match on. Instead the response names the
  difference and which existing act closes it: money missing is a withdrawal
  nobody logged; money extra has no act yet, and it says so plainly rather
  than pretending otherwise.

A real gap found while testing, and fixed. A parent-entered reward is an
earnings-ledger entry, not a chore instance, so it never appears in
settlement's reward_pence and never appeared on the week view at all — the
child's screen said "on track for £1" while £2.50 of reward sat against the
week, owed to him and invisible. payments.ad_hoc_rewards is now public and
the view carries two figures: total_pence, which is what the week settles at
and what a parent agrees to, and payable_total_pence, which adds the rewards
and is what he will actually be handed. The child's headline is the second.
The distinction is real and had to be kept — a reward must not move with the
chore result — but one of the two had to be on the screen, and it was not.

frontend/: parentApi.ts, parentWords.ts, ParentView.tsx, and
components/parent/.

  The PIN is asked for per submission by a dialog that first states what is
  about to happen, and is kept nowhere afterwards. It lives in that
  component's own state, is cleared before the request is awaited, and the
  component is unmounted when the act finishes. No unlocked mode, no
  "remember for five minutes", no header set for later. The API refuses these
  requests without it regardless — this is not what makes the app safe — but
  a screen that stayed unlocked on a kitchen wall the child walks past would
  make the server-side check irrelevant in practice.

  Ruling on the queue decides nothing: it marks items. The consequence is
  fetched, then the batch is submitted once. Changing any ruling clears the
  consequence, because a stale one describes a batch nobody is about to send.

  parentWords.ts phrases consequences and never calculates one. If it did
  arithmetic of its own the sentence and the outcome could disagree, which is
  the single failure this whole design exists to rule out.

  Closed weeks render with no controls at all. Nothing in the app can edit a
  settled week, so nothing there offers to.

Tests: 414 pass, 22 of them new. The card's case is
test_the_stated_consequence_is_what_actually_happens: the consequence is
predicted, the batch is submitted, and the resulting proposal is compared
field by field against what was predicted.

Verified live against a running server, with the parent components rendered
server-side against the real payload so the actual sentences could be read.
A failing week with three claims waiting stated "The chore money comes back:
£1.80, which was at risk. Outstanding misses go from 3 misses to no misses.
The week goes from £1 to £2.80 — up £1.80", and after submitting: total 280,
chore pay 180 awarded, no outstanding misses, queue empty. Also driven live:
a reward typed as "£2.50" landing on the next open week because its own had
settled, a waived day leaving confirmed work alone, settling at the figure
read, paying with £2 banked, a withdrawal, an overdrawn withdrawal refused,
a wrong PIN refused, and all three reconciliation outcomes.

Not done: the API still does not serve the built bundle, so the two screens
run against the Vite dev server. The WEEKLY_CONDITION gap from Session I is
untouched — it is a settlement problem and the parent view is where the
judgement would have to be made, so it is now overdue rather than merely
recorded.

2026-08-21 — Session K: the summary endpoint

GET /api/summary. Seven fields, 195 bytes on the wire, unauthenticated,
written for a tile on somebody else's dashboard rather than for a page.

  week_start, week_end, status, projected_total_pence, projected_total,
  recovery_outstanding, recovery_deadline, days_remaining

app/services/summary.py. Derived from week_view.build rather than computed
beside it. If this said a recovery was outstanding while the kitchen screen
said the week was fine, the tile would be worse than no tile at all — so
there is one definition of "outstanding" and this reads it.

Four decisions, all of them consequences of what reads this:

  1. The figure is the payable total, rewards included: the tile answers "how
     is he doing", not "what will a parent agree to on Sunday". Those two are
     different numbers and Session J made the difference explicit.

  2. days_remaining is always present, outstanding or not. The week runs
     Sunday to Saturday in Europe/London and contains a clock change twice a
     year; a consumer counting days from a date string gets that wrong every
     autumn and never finds out. It goes through calendar.elapsed and is
     floored at zero, so the last day reads 0 rather than a negative.

  3. A week nothing has happened in has no row, and the endpoint answers
     anyway rather than 404ing. A tile that errors every Sunday morning until
     somebody opens the app is a tile nobody trusts by the third week. The
     figure reported is the base allowance, which is not a stand-in: it is
     exactly what propose() returns for a freshly opened week, since
     everything untouched is a prospective miss and the chore pay fails.
     status is "not_started" so a consumer can tell the difference, and asking
     brings no week into being — it is a GET and it writes nothing.

  4. A closed week reads from its own stored columns, and nothing is
     outstanding in a week that is over.

recovery_deadline is the only nullable field, and it is null for exactly one
reason: there is nothing outstanding to have a deadline. Everything else is
present every time, so a tile never has to branch on which keys arrived.

Nothing about authorisation and nothing chore-level, asserted rather than
assumed: a test greps the response body for "pin", "parent", "locked",
"attempt", "instance", "chore" and the names of the chores in the fixture.
The tests assert the whole key set as a set, not the fields they happen to
care about — a key quietly added or renamed is precisely the change that
breaks a consumer nobody in this repository can see.

Tests: 428 pass, 14 of them new. The card's three weeks: a clean one, one with
a recovery outstanding and its deadline, and the last day of one — the last
driven through the service with the clock supplied, since a test cannot wait
for Saturday evening. Sunday morning of a seven-day week reads 6 days,
Saturday evening reads 0, and past the end it stays 0.

Verified live: not_started before anything existed, then a clean week, then a
parent-marked miss turning recovery_outstanding true with the deadline, then
the bonus worked and both going back to false and null with the total moving
£1.00 to £2.00.

2026-08-21 — Session L: the Dockerfile and the deployment entry

app/frontend.py — SPAStaticFiles (falls back to index.html for an unmatched
path outside /api, so a hard reload or a deep link works) and mount_frontend,
mounted last in app/main.py so every API route and /health match first. A
no-op when there is no built bundle, which is how dev and the test suite run.

app/main.py gained a lifespan that runs app.db.run_migrations() before the
app serves anything — alembic upgrade head, idempotent, so `docker compose up`
after a pull is the whole deploy. app/config.py gained frontend_dir
(FRONTEND_DIR, defaulting to frontend/dist, resolved absolute). Verified safe
against the existing test suite: the plain `client` fixture never triggers the
lifespan (no context manager), and every fixture that does trigger it
(`with TestClient(app) as client`) already runs behind the real migration via
the `migrated_database`/`session` fixtures, so the startup migration is a
no-op there too. 428 pass, unchanged.

Dockerfile — two stages. node:24-bookworm-slim builds the frontend;
python:3.13-slim-bookworm runs it, with FRONTEND_DIR, DATABASE_URL and TZ set
as image defaults. No git, no openssh-client: unlike KitchenSync this service
has no vault snapshot and reads no filesystem beyond its own database
(CLAUDE.md: "no external filesystem access, no dependency on any other
service"). Non-root uid 1000, /data created and chowned for the volume mount.
CHILD_NAME and PARENT_PIN are deliberately absent from the image — no default
exists anywhere in the repo, which is public, and the container refuses to
start without both supplied at deployment.

docker/docker-compose.yml — standalone service, one named volume
(coinquest-data) and nothing else mounted, a healthcheck against /health with
a 40s start_period for first-boot migrations, CHILD_NAME/PARENT_PIN required
via `:?` (this compose file is this repo's own — a missing secret failing the
whole `up` is the right behaviour here, unlike the household stack below).

The household stack: AlfredHomeHub's docker/docker-compose.yml gained a
coinquest service (port 8600), built from this repo cloned as a sibling —
../../CoinQuest, override with COINQUEST_REPO_PATH — exactly like kitchensync
and kanban-api. Its four variables are documented in that repo's DEPLOY.md
under a new CoinQuest section, alongside scripts/coinquest-backup.sh,
restore-coinquest.sh and setup-coinquest-backup.sh (mirroring the KitchenSync
three, adapted to one table-free volume and no vault mount), and a nightly
backup cron slot at 03:57 — the next free one after KitchenSync's own backup.
The restore drill checks integrity, the Alembic revision, and row counts
across all seven tables (chore_definitions, chore_instances, weeks,
settlement_lines, waivers, earnings_ledger, savings_ledger).

Not done: no Docker available in this environment to actually build, bring up
or drill-restore the container — every file above is written and the compose
YAML parses, but the acceptance checklist in DEPLOY.md is unchecked. That is
the next thing to run, on a machine with Docker (ideally the Pi itself), before
this is considered deployed.

2026-08-21 — Session L addendum: verified on the Pi, two fixes

Build, health check, LAN reach, persistence, backup, and the restore drill's
negative test all passed on real ARM64 hardware. DEPLOY.md's acceptance
checklist now records it.

Two things the drill on real hardware caught that a file review could not:

1. DEPLOY.md claimed compose picks up this repo's root .env automatically
   when invoked from the repo root. Wrong: compose's project directory is the
   directory holding the -f file (docker/), not the caller's working
   directory, so every ${VAR} silently resolved empty and CHILD_NAME/
   PARENT_PIN refused to start even with the file sitting one level up. Fixed
   by adding --env-file .env to every documented command rather than moving
   the file — the root .env is already the one uvicorn, pytest and npm read,
   and splitting it would leave two files to keep in sync for no reason.

2. The restore drill that passed ran against an empty database — that was all
   that existed at deploy time. It proves the mechanics (extract, open,
   integrity-check, restart) but not that real rows survive. Left open on the
   checklist rather than ticked: it needs a real run with data seeded across
   all seven tables before the backup can be trusted, the same standard
   KitchenSync's own drill was held to.

Pushed on the strength of everything else passing; the seeded-data drill is
recorded as open in DEPLOY.md's checklist rather than closed early.

2026-08-21 — Session R: a parent screen for the chores themselves

Every chore up to now was seeded straight into the database by a test or a
migration; there was no way to add, change or retire one over HTTP or from
the app at all. This card closes that gap for the five cadences the scheme
actually uses, and deliberately not for a sixth.

Read app/models/chores.py and both rules docs before writing anything, per
the card's own instruction. Two questions came out of that and were put to
the user rather than guessed:

  "Specific weekdays" as a cadence turned out not to exist anywhere — not in
  the schema, not in pocket-money-rules.md, where every chore is daily,
  N-times-a-week or week-long. Building it properly needs a new Cadence
  member, a weekdays column, a plan_week() branch, and a real answer for
  whether it belongs in ON_DEMAND_CADENCES and WEEK_DERIVED_CADENCES — none
  of which the rules doc answers, because nothing uses it yet. Deferred by
  choice (recorded in docs/scribble.md) rather than half-built as a form
  option that would silently generate no instances.

  "Administered" was safe to add now: a plain is_administered boolean,
  orthogonal to cadence — the clock test still has a normal frequency, the
  difference is only that a parent marks it rather than the child claiming
  it. Revision 9dbe9580e290 adds the column, server_default '0' so every
  existing chore stays exactly what it always was. The claim endpoint does
  not read the flag yet; today it is a fact the parent screen can show and
  set, nothing more.

app/routers/chores.py — POST/GET /api/chores, POST /api/chores/{id} (edit —
POST, not PUT, matching the fact that no other endpoint in this API uses
anything but GET and POST), POST /api/chores/{id}/retire. All three writes
carry the PIN, the same AuthorisedRequest/authorise() pair every other write
in this app uses — no second mechanism. Retiring sets is_available false and
refuses a second retirement (409) rather than a silent no-op; there is no
delete endpoint at all, matching "a definition that was ever claimed against
must never be removed" — and since nothing distinguishes "never claimed"
from "claimed" at this layer, retire is simply the one operation offered.

The one design decision beyond CRUD: every write re-syncs *today's* open
week immediately (mirroring waivers.py), so a created, edited or retired
chore appears on the current week's view without anyone reopening it. Other
open weeks pick up the change the next time they're touched, which was
already true before this card — plan_week() always reads live definitions.

frontend/src/components/parent/Chores.tsx — a panel wired into ParentView
alongside Rewards/Payday/SavingsPanel/ClosedWeeks, on the same data-refresh
cycle and the same ask()/PinDialog every other act on that screen uses. A
create form and a per-chore edit form share one component; cadence's
shape-specific field (times_per_week) shows only for weekly_count, mirroring
the CHECK constraint the server enforces anyway. Amount is typed as pounds
and parsed client-side with the existing parsePence, the same pattern Payday
and the withdrawal form already use, sent to the API as a plain amount_pence
integer — the router does not accept a pounds string for this one, unlike
/api/rewards, since the card asked for pence and there was no reason to
reopen that design question.

Tests: 451 pass (428 + 23 new in tests/test_chores.py), including the card's
two named cases — a created chore appearing on the current week's view with
no reopen, and retiring a chore that a settled week already paid for leaving
that week's stored figures and its settlement lines byte-for-byte unchanged,
proved through the retire endpoint itself rather than by editing the model
directly. One unrelated pre-existing flake seen once in the full run
(test_parent.py::test_previewing_changes_nothing) and not reproduced on
three subsequent full runs or in isolation — not touched by this card.

Verified live against a running server, not just TestClient: PIN gating (no
pin -> 422, wrong pin -> 401 with the lockout logging its usual "1 of 5"
line, correct pin -> 201), a daily chore appearing on all seven days of the
already-open current week with no reopen call in between, an administered
weekly-count bonus chore, editing its amount, retiring it, a second retire
refused with 409, and the retired chore still listed rather than gone.

npm run build (tsc -b + vite build) clean.

2026-08-21 — Session S: a sixth cadence, chosen weekdays

The deferral from Session R — recorded rather than guessed at — comes back
built. Read app/models/enums.py, instances.py's plan_week(), recovery.py and
week_view.py before writing anything, per the card's own instruction; the
finding that made this small was that both frozensets already do their job
by membership alone, and week_view.py's day/week split already keys off
`due_date is not None` rather than switching on cadence — so a day-scoped
cadence slots in without touching either file at all.

Cadence.WEEKDAYS added to enums.py, and to both WEEK_DERIVED_CADENCES and
ON_DEMAND_CADENCES, exactly as directed — "settled, not a question to
reopen." app/models/chores.py gains `weekdays` (a comma-joined, Monday-first
canonical string over WEEKDAY_TOKENS — date.weekday() order, index-compatible
with week_view.WEEKDAY_NAMES) plus parse_weekday_tokens/format_weekdays/
parse_weekdays, and a CHECK mirroring times_per_week's exactly: required for,
and only for, weekdays. Revision 4872595d1036.

instances.py's DAILY branch became _day_scoped_instances(definition, week,
waived_days, weekday_filter=None), called by DAILY unfiltered and by the new
WEEKDAYS branch with the definition's own set — the filtering the card asked
for, not a duplicated loop. A waived day excuses whatever was due on it
either way; a waived day the chore was never due on changes nothing, proven
by a dedicated test.

No changes needed in recovery.py or week_view.py beyond the enums — verified
rather than assumed: assess_week()'s `required = plan.required` already
reads however many PlannedInstances plan_week emitted for a WEEKDAYS
definition, which for a 2-day chore is 2 every week (every week contains
each weekday once), never 7. can_be_spent_as_recovery and the recovery
panel's `options` already gate on ON_DEMAND_CADENCES membership plus an
actual claimable instance existing — which for WEEKDAYS only exists on its
own due days, so "on demand" never overclaims a day it isn't due.

app/routers/chores.py: ChoreWriteRequest gains `weekdays: list[str] | None`,
validated and canonicalised through parse_weekday_tokens/format_weekdays in
the same cadence_carries_its_own_fields validator times_per_week already
used — cadence carries its own shape-specific fields, weekdays is just a
second shape. ChoreDefinitionView outputs it as a list, not the stored
string. _resync_current_week() needed no changes at all: it already re-plans
today's week from live definitions on every write, and plan_week's new
branch is what makes that correct for the new cadence too.

frontend: parentApi.ts gains Weekday, WEEKDAYS (Monday-first, matching the
server) and ChoreDefinition.weekdays. parentWords.ts's cadenceLabel takes an
optional weekdays list and reads "Tuesday and Friday" rather than a token
list. Chores.tsx gains a weekdays option in the cadence picker and a day
checkbox row, shown only when that cadence is selected — same pattern as the
existing times_per_week field. npm run build clean.

Tests: 469 pass (451 + 18 new) — test_recovery.py's exact-membership test for
ON_DEMAND_CADENCES updated to include WEEKDAYS (this is the one place a
frozenset's literal contents were asserted, and the card said this
reopening was intended), a new equivalent test for WEEK_DERIVED_CADENCES,
plan_week() coverage for the new branch including the two waiver
interactions and the October clock-change week, a full recovery-eligibility
test proving a completed weekdays bonus chore rescues a miss exactly like a
daily one, and in test_chores.py: validation (weekdays required and only for
that cadence, unrecognised token refused, canonicalised regardless of input
order), the card's two named cases — a Tue/Fri chore due on exactly those
two days with no reopen, and a missed Tuesday recovered by a bonus chore,
proven through the recovery panel — and a week settled before this card
shipped, with a weekdays chore only entering the scheme afterward, left
byte-for-byte unchanged.

Verified live against a running server: created a Tue/Fri chore (input
"friday,tuesday", stored and returned canonically as "tuesday,friday"), the
week view showed it due on exactly those two days and no others, marking
Tuesday missed and completing a bonus chore brought the recovery panel to
outstanding:0 covered:1 "Bins out -> covered by Water houseplants", and
editing the chore afterward to Monday/Thursday added those as new untouched
instances while leaving Tuesday's already-missed one exactly as it was —
"never destroy work" holding for the new cadence without any code written
specifically to make it do so; it was already the rule.

2026-08-21 — one shared pot, not one price per basic chore

Not a carded session — an unplanned follow-on prompt, arising from real use
rather than a board card of its own. It refined Session R's chores screen:
building it was what let a parent type real amounts into more than one
basic chore for the first time, which is what surfaced the gap below.

Real use surfaced a real gap: "Make bed" at £2 basic and "Lunchbox and cups"
at £2 basic read as £4 owed, when pocket-money-rules.md §2 describes one
£2 pot the basic chores collectively gate. ChoreDefinition.amount_pence's
own comment already said "for a basic chore this is its share of the weekly
chore pay" — nothing enforced the "share" part; recovery.py just summed
every assessed basic requirement's own amount.

app/models/settings.py — SchemeSettings, one row, seeded to 200 (£2) by
revision a4a5f729d606. Named apart from app.config.get_settings()
deliberately: that is env-var configuration needing a restart; this is
reviewed and edited through the app, in the database, like a chore
definition is. app/services/scheme_settings.py reads it (get_row,
weekly_basic_pay_pence) — the one place both settlement.py and the new
router touch the table, so there is nowhere for the two to disagree about
its shape.

app/routers/settings.py — GET /api/settings (no PIN, same as every other
read), POST /api/settings (PIN-gated, the usual AuthorisedRequest/
authorise() pair). No re-sync on write, unlike chores.py: this figure is
read live at proposal time, never baked into a stored instance, so the next
GET already sees it.

recovery.py: WeekAssessment gained weekly_basic_pay_pence, required with no
default — a wrong silent default here is a wrong figure of real money.
chore_pay_at_stake_pence returns the configured pot when at least one basic
requirement is_assessed, else 0 — not a sum of amount_pence any more. Every
caller of assess_week() had to start supplying the new argument, which is
what forced the audit that found how far the old per-chore-amount reading
had spread.

app/routers/chores.py: amount_pence is now optional, and category-gated —
omitted or 0 for basic (the stored value becomes 0 either way), refused
outright if a basic chore is given a nonzero one rather than silently
discarded. Silently ignoring a typed-in figure is exactly the shape of bug
this session exists to fix, just wearing a different disguise. Still
required for bonus and reward, unchanged.

The blast radius that made this a bigger session than it looked: grepping
every use of chore_pay_at_stake_pence found four existing tests
(test_settlement.py, test_week_view.py) that asserted the OLD sum as
correct — the bug, encoded as a passing test. Fixing those surfaced a second,
undocumented gap the prompt's own wording hadn't named: settlement.py's
_lines() built each basic chore's settlement line from its own
amount_pence too (`paid = requirement.amount_pence if chore_pay_pence else
0`), which — now that amount_pence is 0 for every basic chore — would have
made every basic line read "£0.00" even in a week the pot fully paid,
silently breaking test_every_stored_amount_is_an_integer_number_of_pence's
enforced invariant that stored lines sum to the non-base total. Fixed with
one more line per settled week: "Weekly basic pay", tied to no single
definition (ProposedLine.definition_id loosened to int | None, mirroring
SettlementLine.source_definition_id's existing nullability for a deleted
chore), cadence=WEEKLY_CONDITION as the closest true description of what is
happening to it — judged once, at settlement — not because it is one. Every
individual basic chore's own line now reads 0 with a status note; the pot's
own line carries what it actually paid. Verified live: a settled week's
lines read "Make bed 0p / Lunchbox and cups 0p / Hoover downstairs 0p /
Weekly basic pay 200p" — summing correctly, and readable as an honest record
of who did what plus what it was worth.

Test fixtures across test_settlement.py, test_week_view.py, test_payday.py,
test_override.py and test_parent.py all constructed a "basic chore worth
Np" and expected N in the total — every one updated to zero the chore's own
amount and configure scheme_settings.weekly_basic_pay_pence to N instead,
preserving each file's existing arithmetic rather than rewriting every
downstream assertion. One genuine behaviour change beyond bookkeeping:
test_parent.py's waiver test used to assert that waiving one of two basic
chores reduced chore_pay_at_stake_pence by that chore's amount — under the
pot model waiving one of several basic chores that still leaves at least
one assessed changes nothing, which is the correct new behaviour and the
test now says so.

Tests: 488 pass (469 + 19 new) — tests/test_scheme_settings.py for the
endpoint (seeded default, PIN required to write and not to read, negative
refused, zero allowed), new coverage in test_recovery.py for
chore_pay_at_stake_pence directly (the configured pot regardless of how
many basic chores or their own retired amounts, zero when nothing basic is
assessed, does not grow with a second basic chore), three
test_settlement.py cases matching the prompt's own test plan verbatim (three
basic chores paying the pot once and not three times, an unrecovered basic
miss still paying nothing, bonus and reward chores paying their own amount
untouched), and six in test_chores.py for the router's validation (basic
defaults to 0, basic with 0 is fine, basic with a nonzero amount refused on
both create and edit with nothing created, bonus and reward still refused
without their own amount).

frontend: Chores.tsx's amount field is gone from the form for basic
category — hidden, not disabled, and `ready` no longer requires a valid
amount when the category is basic. A new WeeklyBasicPay control sits at the
top of the chores section: the current pot, an Edit toggle, and a PIN-gated
save through the same ask()/PinDialog as every other act on this screen.
Each basic chore's row now reads "shares the weekly basic pay" rather than
a per-chore amount that would always show £0.00. parentApi.ts's
ChoreWrite.amount_pence is now nullable to carry the "no amount" case
through the same shape create and edit already share.

Verified live against a running server, the whole path end to end: three
basic chores created with no amount field sent, a bare nonzero amount for
one of them refused (422) before it ever reached a definition, all three
confirmed, chore_pay_pence read 200 (not 600), settled, and the settlement
lines read exactly as designed. A bonus and a reward chore created
afterward still required and kept their own individual amount, unaffected.

npm run build clean. pytest 488 passed.

2026-08-21 — a colourful restyle for the child's screen

Not a carded session — an unplanned follow-on prompt, not arising from any
particular gap but a plain request to make the screen look better. It
refined Session I's week view, the child-facing screen this restyles, and
carried the same accent treatment onto Session J's parent view besides.

CSS only — no component touched. The child's screen is now a bright,
kid-facing "game" surface: a two-colour gradient hero card for the projected
total (the one figure the whole screen exists to answer), a colour per
weekday on the day cards (purely decorative — the heading already names the
day, so nothing is read through colour alone), a gradient masthead, and a
warmer fill on the one tappable thing on the page. The parent screen picked
up the same accent family — a soft top strip on every panel, an indigo
primary button — but stayed dense and calm; it is a PIN-gated working
screen, not a toy. Every existing rule held: colour is never the only
carrier (state labels are still words first), the done/todo/urgent/away
semantics are untouched, dark mode is fully defined alongside light, and no
tap target or type size moved.

Verified live rather than by reading the CSS: migrated a scratch database,
ran the real backend and Vite dev server, seeded chores through the API,
drove a claim through to confirm/reject with Playwright, and screenshotted
both screens in light and dark. That caught one real contrast fault — the
"Bonus" tile in the totals card read lower-contrast than "Every week"
because a light overlay washed out on the coral end of the gradient — fixed
by switching to a dark overlay, which holds contrast consistently across
the whole gradient regardless of which end a tile sits on.

npm run build clean.

2026-08-25 — Session V: reopening a settled week, narrowly

"A settled week is a closed event" has held since the first migration —
enforced by a 409 at the settle endpoint, by propose() itself raising on a
closed week, and by settlement lines surviving a deleted chore definition.
That rule exists to stop a scheme change made months later reaching
backwards. It was never meant to stop a parent correcting their own mistake,
made on purpose and later regretted, and until now it did. This card adds a
reopen — deliberately narrow, in every direction the card named.

app/models/weeks.py — WeekReopening, a new append-only table alongside
SettlementLine: who reopened a week, when, why, and a full copy of what stood
immediately before (every settled figure, the void reason if it was voided,
the override if it was overridden, and whether it had been paid). A copy, not
a lookup — the week's own columns move on the instant the reopen closes, the
same reason SettlementLine copies a chore's name rather than joining to it.

Migration 6131c9c1284a. Three things land together:

  1. The weeks trigger gains one controlled exception, defined structurally
     rather than by a flag: a transition to `open` that clears every closing
     figure — status, all four settled_* columns, closed_at, and any
     override — in the same UPDATE is let through; anything else touching a
     closed week's guarded columns still aborts exactly as before. A tightened
     CHECK (closed_weeks_carry_their_figures) now requires an open week to
     carry none of its parts, not only no total, which is what makes "cleared"
     checkable at the row level rather than a convention the trigger merely
     trusts. Proved both ways: the existing raw-SQL "flip status alone"
     regression test still aborts, and a real reopen through the ORM (which
     clears every guarded column in one flush) does not.
  2. week_reopenings itself, with its own append-only triggers and a CHECK
     mirroring an_override_that_costs_money_says_why exactly: reason required,
     blank or whitespace refused at the database layer as well as the routine.
  3. savings_ledger gains REVERSAL as a second entry type allowed to be
     negative, alongside WITHDRAWAL — a distinct fact from a withdrawal (the
     child did not choose this) sharing only the sign.

app/services/settlement.py — reopen(). Refuses an open week (NotClosed), a
blank reason (ReopenNeedsAReason), and — the rule this card led with — a week
with any settled or voided week after it (NotTheLatestSettledWeek), checked
by querying for a later closed week rather than trusted to the caller, the
same discipline NotOpen already follows. If the week was paid, `savings.
record_reversal()` appends a negative entry for exactly `week.deposited_pence`
— never the whole payment, only this week's apportioned share, which is
already all that column ever held — and refuses (SavingsError, 409) rather
than overdrawing the account if the deposit has already been spent elsewhere;
the reopen is refused whole rather than left half-applied. Every closing
column is then cleared in one flush: the controlled path the trigger's
exception describes. `current_lines()` reads a resettled week's lines back
scoped to since its last reopen — old lines are never deleted, so a week
reopened once carries both rounds forever, but "what does this figure consist
of" means the current round, not their sum.

app/routers/weeks.py — POST /api/weeks/{id}/reopen, PIN-gated through the
same AuthorisedRequest/authorise() pair as every other write, subject to the
existing per-source lockout (a read stays open: nothing here changed that).
Returns the fresh proposal, since the week is open again. SettledWeekView
gained `reopenings` (the full history) and its `lines` now come from
current_lines() rather than every line the week has ever carried; ProposalView
gained the same `reopenings` field so a week that has been reopened but not
yet resettled still shows why, even though it currently reads as an ordinary
open proposal. Re-settling and re-voiding go through settle()/void()
unchanged — no new argument, no shortcut — proved by reopening a week, having
a confirmation change underneath it, and watching a stale agreed-figure
refuse with the ordinary 409, and by a void after reopening still refusing an
assignment.

frontend: parentApi.ts gained reopenWeek(), loadWeekView() (GET /api/week/
{id}, the same shape the child's screen reads, for a week that is open but is
not the calendar's current one), and Reopening on SettledWeek. ThisWeek.tsx
took an optional `title` prop so the same component — chore management,
mark-missed, waive, settle, void, all of it — renders for a reopened week
without reading as "This week". ParentView.tsx now fetches the view for every
open week that is not the current one and renders each under "Reopened week";
more than one can exist at once, since reopening one closed week can make an
earlier one eligible too. ClosedWeeks.tsx computes which closed week is
eligible (the one with the latest start_date — the same rule the server
enforces) and offers Reopen only there, inside that week's own expanded
detail, never beside Settle. The confirmation states the figure being undone
and, when the week was paid, that the deposit is being reversed and that cash
already handed over is not being clawed back by the record changing. Past
reopenings render as history in the same detail panel.

Tests: 510 pass (488 + 22 new, tests/test_reopen.py). The card's own test plan
verbatim: settle, pay, reopen, savings balance back via a reversal entry (the
original deposit row byte-for-byte unchanged), a confirmation corrected,
settled again at a different figure with the reopening still visible in the
week's history; and refusal to reopen the week before the latest one through
every route — the API (409, named) and settlement.reopen() called directly
(NotTheLatestSettledWeek). Also: only a paid week's apportioned share reverses
when one payment cleared two weeks (proved by settling and paying both
together, then reopening the later one and checking the earlier one's deposit
is untouched); a reopen that would overdraw savings refused whole; blank and
whitespace-only reasons refused by pydantic, by the routine, and by the CHECK
constraint via raw SQL; the PIN lockout counts a wrong reopen attempt exactly
like any other write while reads never touch it; an open week cannot be
reopened; and the ordinary immutability trigger still aborts a hand-typed
status flip that does not clear the row the way a real reopen does.

npm run build clean. alembic check: no drift.

2026-08-25 — Session V addendum: a production deploy failure, and the fix

Pushed and deployed the above to the household stack. The migration failed
against the real database: rebuilding `weeks` (the trigger and CHECK changes
above) means dropping it, and SQLite refuses to drop a table another table's
row still references with ON DELETE RESTRICT while foreign key enforcement is
on — which `chore_instances` already did, the moment a real chore had been
claimed against a real week. The container crash-looped on retry; every test
fixture in this project migrates an empty database once, in one shot, so
nothing had ever exercised this. Worse: the failed attempt had already dropped
the `weeks` and `savings_ledger` immutability triggers before it errored, and
never got to recreate them — the live database briefly had no append-only or
closed-week protection at the database level at all.

Recovery, in order: stopped the crash-looping container. Copied the fresh
pre-deploy backup off the Pi entirely — CoinQuest's offsite (restic → B2) leg
turned out to be unconfigured (`RESTIC_REPOSITORY unset` in every backup log
line to date), so every backup that has ever existed, including this one, had
only ever lived on the same box as the volume it protects. Snapshotted the
damaged volume as evidence before touching it, and copied that off too — a
restore over it would have destroyed the only record of what the failed state
actually looked like. Reproduced the failure faithfully in a scratch Docker
volume on the Pi itself (never touching the live one), which is what found
the real root cause and disproved a false lead: a first local check on a
laptop appeared to succeed, but only because it ran plain `alembic upgrade
head` directly, which never imports app.db and so never turns foreign keys on
at all — a real deploy always does, because app.db is already imported by the
time app.main's lifespan calls it. The fix (app/migrations/env.py, PRAGMA
foreign_keys toggled on the raw connection before Alembic opens its own
per-script transaction — see that file's docstring) was proved against a copy
of the real production data, on the real image, before it went anywhere near
the live volume a second time. Restored the live volume from the clean
backup, pulled the fix, rebuilt, and redeployed. Verified afterwards: all ten
tables and ten triggers present, `PRAGMA integrity_check` ok, `PRAGMA
foreign_key_check` empty, every real row — two weeks, thirty-two chore
instances, three chore definitions — intact, and the API serving them
correctly.

tests/test_migration_against_real_data.py makes the gap permanent rather than
a one-off check before this one redeploy: it seeds every table that exists
straight after the first migration with rows that reference each other the
way production data does, then runs every remaining migration through
app.db.run_migrations() itself — not a bare alembic call — so it actually
exercises the same foreign-key behaviour a real deploy does. Confirmed to
fail without the fix and pass with it. The base and head revisions are read
from the migration scripts themselves, so it never needs updating as new
migrations are added.

Found and logged in docs/scribble.md, not fixed here: 1eb8e8b3e4ae's
downgrade() is separately broken against real data (an unrelated, pre-existing
gap surfaced while proving the fix above) — its batch_alter_table rebuilds
weeks without an explicit copy_from, so Alembic's reflection carries a CHECK
constraint referencing a column the same downgrade just dropped. Never caught
before because downgrades are not part of this project's deploy story.

docs/progress.txt's own session letters were also found to be wrong while this
was being written — see the correction and the convention note now at the top
of this file. Housekeeping only, done once the redeploy above was verified
and not before: nothing in it changes any code.

512 tests pass (510 + 2 new). alembic check: no drift.

2026-08-26 — Session T: the Claims Waiting section had no confirm button in it

A functional defect, reported against the deployed app on 23 August: pressing
Yes on a pending claim staged it, and then there was nothing in the Claims
Waiting section to commit with.

Diagnosed first, against four named hypotheses, before touching any code —
not by reading the source but by driving the real app in a real browser
(Playwright, added for this) against a seeded database of pending, already-
confirmed and previous-week claims: the built production bundle, not the dev
server, since that is what a deploy actually serves. Every hypothesis
checked out clean. The frontend called POST /api/claims/review every time.
The dialog submitted. A genuine race — an item confirmed "elsewhere" between
preview and submit — came back a clean 409 and the frontend surfaced it,
both at the preview stage and the commit stage. The queue updated with no
reload, and a real reload after confirmed the state had persisted. git log
on every file in the flow showed nothing had changed since Session J
(2026-08-21) built it, so whatever ran on 23 August is exactly what this
tested. Tested again on an iPad-emulated touch viewport, since this runs on
a kitchen wall screen — same result.

The actual defect, once described precisely: after staging a claim, the
*only* visible control was "What does this do?" — a button that reads as a
question, not a commit. The button that actually commits, "Submit with
PIN", did not exist in the DOM at all until that first button was clicked.
A parent looking for something to confirm with, right after ticking Yes,
found nothing there to confirm with — which is a true statement about the
screen at that moment, not a misreading of it. Every other act on this
screen — Settle, Void, Mark paid — goes from one clearly-labelled button
straight to the PIN dialog, which shows the consequence before asking for
the PIN. Queue.tsx was the one flow that broke that pattern with an extra,
separately-labelled, easy-to-miss step in between.

Queue.tsx: the two-state, two-click "preview then submit" dance is gone.
One state (`problem`, for a refusal), one button ("Confirm"), reachable
the instant something is staged. Clicking it fetches the same preview the
old flow did and hands the result straight to `onSubmit`, which
ParentView.tsx's `submitBatch` already turned into a PinDialog — unchanged,
since a dialog carrying a summary and consequence lines is exactly what
Settle and Void already open. The impact readback is not gone; it lives in
the dialog now, the same place every other act's does, read before the PIN
is typed rather than in a separate display that needed a second button to
reach. Batch semantics are untouched: still one PIN, one transaction, all
or nothing, through the same POST /api/claims/review this always used.
Removed `.effect`/`.effect h3`/`.effect ul`/`.effect li` from App.css along
with the JSX that was their only reader.

frontend/e2e/ — a permanent Playwright suite, not a one-off check. Builds
the real bundle, seeds a scratch database via seed_database.py (a pending
claim in the current week, one already confirmed, one pending from the
previous week — the exact mix the card's test plan asked for), and starts
the real app against it in one script (start-server.mjs) rather than
trusting Playwright's globalSetup/webServer ordering, which turned out not
to guarantee the database exists before the server tries to open it.
confirm-flow.spec.ts drives the actual flow: confirm the previous week's
claim and reject the current week's in one mixed batch, assert it commits
with no manual reload and survives a real one; and, separately, a batch
raced into refusal by another request mid-flow, asserting the PIN dialog
never opens and the reason lands on the screen. Proved as a regression
test rather than a demonstration it once passed: reverted Queue.tsx to the
broken version and confirmed both cases fail (one on a missing "Confirm"
button, one on a 30-second timeout waiting for one that never appears),
then restored the fix and confirmed both pass again. `npm run test:e2e`
from frontend/, documented in CLAUDE.md.

512 backend tests still pass, untouched — this was a frontend-only defect.
npm run build clean.

2026-08-26 — Session U: the on-track figure comes from the engine, the
parent view groups by week, and both screens page back

Part 1. Diagnosed rather than assumed: a fresh, entirely untouched week's
proposal came to 100p (the base allowance alone), not 300p (base plus the
whole chore pot), proven with a scratch script against production-shaped
settings before touching anything. The cause was not the view bypassing the
engine — week_view.build already called settlement.propose — it was
assess_week itself. `propose()` is used two ways that need opposite answers
to the same question: settle() needs "what would this week pay if it ended
right now", which is correctly pessimistic — everything still untouched is a
prospective miss. A screen read mid-week needs "where does the child stand
before anyone has ruled on everything", and treating a chore not yet due, or
simply not yet done, as an immediate miss answers a question nobody asked.
`recovery.assess_week` gains `for_display`, threaded through
`settlement.propose`: pending CLAIMED instances count toward what was
confirmed (a parent's rejection is what reverts that, not their silence),
and only a miss a parent has actually marked counts against the chore pay —
exactly `Miss.is_definite`, the set the recovery panel has used since
Session I for the identical reason. `_misses_for` gained
`infer_remainder`, because passing an empty `untouched` list on its own was
not enough — it still manufactured misses with no instance to point at.
`for_display` defaults off everywhere except `week_view.build`, which
defaults it on: the router's proposal endpoint, `settle()`, `void()`, and
`parent.preview()`/`parent.pending()` are entirely unchanged, so what
actually settles is still exactly as pessimistic as it always was. Added
`?for_display=` to GET /api/weeks/{id}/proposal so the two figures are
independently readable and testable.

A real consequence of the split, caught by the test suite rather than
assumed away: several existing tests read the child's own screen
(`/api/week`) to get the figure to *settle* with. That figure is now
honestly allowed to differ from what settlement would actually pay, so
those tests — and, more importantly, ThisWeek.tsx's own Settle button —
were reading the wrong number. Settle now fetches the true, pessimistic
proposal fresh the instant it is pressed (GET /api/weeks/{id}/proposal,
default), and only then opens the PIN dialog on that figure — the same
"ask, then show the consequence, then the PIN" shape Queue's Confirm
already uses. Tests fixed the same way: settle on the proposal endpoint's
figure, never the week view's.

Totals gained `held_as_makegood_pence` (a bonus chore worked and given up,
summed from `proposal.recoveries` on an open week, read back from
settlement lines' `unit_amount_pence` on a closed one) and Total.tsx shows
it as its own line — a bonus that is spent rather than paid must say so, or
it reads as simply ignored.

A side effect worth recording precisely rather than losing: the
WEEKLY_CONDITION gap from Session I (a condition scores as a standing miss
from the first minute of the week, because nothing can ever rule it either
way) is now half-cured. `for_display` never infers a miss from silence, and
nothing can mark a condition missed since it has no instance, so the
child's own screen no longer shows the bonus being quietly eaten by a miss
he was never told about. Settlement's own reckoning is untouched and still
carries the bug — proven in the same test, both figures asserted, so the
gap stays visible instead of accidentally vanishing along with the display
symptom.

Part 2. Queue.tsx groups pending claims under a heading per week, most
recent first — display only; a ruling on a claim from any week still joins
the same one batch, submitted together with the same single PIN, exactly as
before. PendingClaim/PendingView gained `week_end_date` so the heading uses
the same `weekLabel` wording as every other week reference on this screen.
"Any week that has not settled stays actionable, however old" needed no
backend change: `parent.pending()` already queried every OPEN week, not
just the current one — the flat list was the only thing hiding it.

Part 3. week_view.build() now handles a closed week itself rather than
letting propose()'s NotOpen refusal reach the router as a 409: it reads
`settlement.stored_figures()` and `current_lines()` instead, never calls
propose() or assess_week on a closed week, and returns an empty recovery
panel (nothing to act on). `_weekly()` lost its dependency on a proposal
entirely — it reads `plan.required` directly, which is what
`WEEK_DERIVED_CADENCES` assessment already sourced it from, so the same
function now serves an open and a closed week alike. WeekView gained
`is_current` (the week's start date against `calendar.current_week`) — the
one field a screen paging back needs to say plainly it is not looking at
now, since a wall screen left open on last week reads as today otherwise.

frontend/src/components/WeekNav.tsx: shared by both screens.
`WeekNav` pages to the nearest existing row, never a calendar date nobody
has touched — a week only exists once it has been opened.
`NotCurrentBanner` is the loud half, reusing the same colour a waived day
already reads in (set apart from the ordinary flow, not an alarm and not an
achievement) rather than inventing a new one.

Child's screen: paging back is read-only regardless of the paged-to week's
own status — a week still technically open is not "now" on this screen, so
`asReadOnly()` forces every card's `can_claim` false before rendering,
independent of what the engine says `can_claim` is. That is a presentation
rule about browsing history, not a scheme rule, and it stays in App.tsx
rather than leaking into `can_claim`'s own meaning.

Parent's screen: components/parent/WeekBrowser.tsx replaces the plain
"This week" panel with a navigable one. Paging to an open week (reopened,
or simply never settled) reuses ThisWeek with full controls; paging to a
closed one reuses ClosedWeeks' own detail component, exported rather than
duplicated, so a week's record reads identically whether reached by
expanding it in "Closed weeks" or by paging back to it — including Reopen,
offered under the same eligibility check either way
(`mostRecentClosedWeek`, also exported, so the two call sites cannot
disagree about which week that is). The existing "reopened weeks" list and
"Closed weeks" accordion are untouched: an open week needing attention
still surfaces immediately without a parent having to think to page back to
it.

Total.tsx reads its own status: "On track for" only while open, "Settled
at" or "Voided at" once closed — a permanent figure read in the present
tense as though still moving was the wrong note paging back to history.

Verified live, not only through tests: built the real bundle, ran the
existing e2e seed (two open weeks, a pending claim in each), settled the
older one through the running API, and drove both screens with Playwright,
screenshotting each step — the fresh current week reading "On track for
£3, base £1, chores £2, bonus £0"; paging back showing the "Not the
current week" banner, a read-only "Missed" chore with no button, and
"Settled at £1"; the parent's queue grouped under a week heading; and the
parent's own WeekBrowser paged back to the same settled week rendering
identically to expanding it in Closed weeks, Reopen included.

Tests: 514 pass (512 + 2 new — test_week_view.py's is_current and
still-open-past-week cases). Several existing tests rewritten rather than
merely re-asserted: the card's own test plan — a fresh week, a bonus claim,
a confirmed miss, a make-good, settlement, each step checked against the
settlement proposal — is now
test_a_week_with_a_miss_a_recovery_and_a_waived_day in test_week_view.py.
npm run build clean. npm run test:e2e passes, unchanged, proving the
regrouped Queue still commits a mixed batch exactly as Session T fixed it
to.

Pushed and deployed to the household stack the same day. No migration in
this session, so the usual pre-migration backup was not strictly required —
taken anyway (scripts/coinquest-backup.sh, cheap insurance against real
data). Pulled both repos on the Pi, rebuilt with `docker compose ... up -d
--build coinquest` (ARM64, built on the Pi itself), container came up
healthy with a clean log — alembic ran and found nothing to do, confirming
no schema drift. Checked against the real household database rather than
only the container's health: /api/summary read £3.00 on the real current
week (base plus the whole chore pot, on track — the exact figure Part 1
exists to produce, now live rather than only tested), and /api/week/1 —
last week, real data, genuinely never settled — read is_current: false,
status: open, confirming Part 3 against a week that was already sitting in
exactly the state item 6 describes, not a constructed one.


2026-08-26 — Session Y: a miss is a tap, and taking it back is a parent

The asymmetry the card exists to build, and everything else follows from it:
anything that costs him money is a tap, anything that gives it back is a
parent. Marking a chore missed no longer carries the PIN. Clearing that mark
does, and is a new endpoint.

Part 1. app/routers/claims.py's docstring stated the old rule — "everything
that could turn into money — confirming, rejecting, marking a chore missed —
carries the PIN". That sentence is quoted in the module rather than deleted,
followed by the rule that replaced it and the reasoning: a miss is a proposal
in exactly the sense a claim is, it pays nothing, settlement is what pays and
is PIN-guarded with Session V's reopen behind it. The requirement is the ten
seconds a parent has at the sink while noticing that yesterday was missed,
which four digits on a wall screen defeats — and a miss not recorded on the
day is a recovery window the child never gets told about, which is the whole
thing the window is for.

A consequence that was not optional and is worth recording: `authorised_by`
is now cleared rather than filled in. Nobody proved themselves, so nobody is
named — the same reason `record_inferred_misses` clears it (a parent who
rejected a claim on that instance earlier in the week is still sitting in the
column, and leaving them there reads as though they had ruled it missed).
That met a CHECK constraint from 82826e03b64d saying a parent-marked miss
names its parent, which was right while marking one was a decision somebody
had to prove themselves to make and is now a demand for a name that does not
exist. Migration c3f8b21a7d40 drops it. Its siblings are untouched: an
inferred miss still has no author, a confirmation still names one, and
MissOrigin.PARENT_MARKED still means what it always meant — decided while the
week was open, as against inferred from silence once it closed — which is
what `Miss.is_definite`, the recovery window and the new clear endpoint all
still turn on.

The migration rebuilds chore_instances from an explicit `copy_from` rather
than by reflection, because SQLite cannot drop a CHECK and reflecting the
table would have re-run every other constraint name through the metadata
naming convention a second time. That is exactly how the stored names came to
read ck_chore_instances_ck_chore_instances_… in the first place: batch
migrations after the initial schema each added a prefix. Verified by dumping
the real DDL before and after — every remaining constraint byte-for-byte
identical, and both partial unique indexes (definition/day, and
definition/week/sequence) intact, since a rebuild that reproduced every
constraint but silently dropped the indexes would be the worst kind of
correct. `alembic check`: no drift.

Part 2. POST /api/instances/{id}/missed/clear. Returns the instance to
UNTOUCHED and clears missed_at, miss_origin and authorised_by; refuses a
closed week exactly as mark_missed does; refuses anything not currently
missed; and refuses a miss whose origin is INFERRED_AT_SETTLEMENT — that one
belongs to a week already closed, so saying it by origin says out loud what
the closed-week check would otherwise only say by accident. rejected_at and
rejection_count survive: clearing a miss does not unhappen a rejection, the
same rule re-claiming already keeps.

Part 3. The control is on the day tile. frontend/src/components/Chore.tsx
grew a second control set deliberately apart from the row: small, under the
chore rather than being it, and worded as a statement rather than an
instruction. Every other tappable thing on this screen is a full-width 3.5rem
row because a child in a hurry does not aim — which is precisely why this one
is not, since the child reaching for "I've done it" must not land on it. A
missed instance renders as missed and carries "Not missed after all", which
opens the parent views' own PinDialog, unchanged: it states the consequence,
holds the PIN for the length of the call, and unmounts with it. Both controls
are passed only for the current, open week, so a week paged back to stays
read-only — the same rule `asReadOnly` already applies to claiming.

The parent screen's own "Mark something missed" control lost its PIN dialog
too. It had to: the API no longer checks that PIN, and a dialog that asks for
one it will not check teaches a parent something untrue about what is
guarded. It kept its place because it reaches a reopened week and a chore
with no day attached to it. ThisWeek/WeekBrowser gained an `onDone` prop for
the reload the dialog used to do on the way out.

Part 4. The figure moves by re-reading the engine, never by subtracting.
Every act on the child's screen reloads the whole view, which re-reads
`settlement.propose(for_display=True)` — so one confirmed miss removes the
whole £2 chore pot and leaves the £1 base because that is what the
all-or-nothing rule says, not because a view knows to take £2 off.

Part 5. The make-good line, on the banner rather than the day tile. A route
back is a fact about the week, not about the Wednesday that happened to be
marked, and it has to still be readable on Friday by somebody who has just
walked past the wall and marked nothing. It is one line — what to do, and
what it takes the week back to — set inside the same card as the figure it
restores, deliberately not a panel and not a second screen.

Both halves come from the engine. `recovery.best_make_good` is the same
optimiser run once more against a week that has not happened yet: "if these
bonus chores were completed, what would `best_assignment` do with them". So
the cap, the all-or-nothing pot, and the optimiser's own refusal to spend a
300p bonus rescuing a 200p pot all apply to the suggestion without being
restated anywhere. It returns None — which renders as nothing at all rather
than an empty encouragement — when nothing is currently lost, when the misses
are past the cap and no amount of work recovers them, when there are not
enough bonus chores left undone, or when the best swap does not actually pay
more than doing nothing. week_view adds the ad-hoc rewards so the figure it
offers is the same figure the banner is showing, and withholds the line
entirely unless the chore it names is on the screen with a button on it:
advice pointing at something that cannot be tapped is not advice.

Part 6. Checked, as the card asked: nothing commits on the displayed figure.
ThisWeek's Settle button still fetches GET /api/weeks/{id}/proposal fresh at
the moment it is pressed (no `for_display`, so the pessimistic reckoning) and
submits that proposal's `total_pence` as `agreed_total_pence`. Untouched by
this session, and now covered by a test that makes the difference visible
rather than assumed: mark a miss, confirm the screen's figure and the
settlement proposal have moved apart, watch a settle at the screen's figure
come back 409, and settle at the proposal's — which pays the base and nothing
else, because the pot is all or nothing and this week did not get it.

Tests: 524 pass (519 + 5 new), plus 4 Playwright specs. The card's own test
plan is now e2e/wall-screen-miss.spec.ts, driving the real built app in a real
browser: £3 on a clean week, mark Wednesday's basic chore missed from the day
tile, the figure drops to £1 with no dialog and no reload, the make-good line
appears naming the bonus chore and the £3 it restores, the undo opens the PIN
dialog, a correct PIN takes the week back to £3 and it survives a real reload
— and a second case where a wrong PIN is refused server-side, said on the
screen, and gives nothing back. Proved to be a regression test rather than a
demonstration: with the day tiles' control removed the spec fails, and passes
again restored. The e2e seed gained one bonus chore, which is what makes a
make-good possible at all; it adds no claim, so the confirm-flow spec's queue
is exactly what it was. npm run build clean.

Pushed and deployed to the household stack the same day. This session has a
migration that rebuilds a table, which is exactly the shape that broke a real
deploy on 2026-08-25 — so the pre-migration backup was taken first and then
actually used as the "before" figures rather than only kept
(coinquest-2026-08-26-1859.tar.gz). Pulled CoinQuest on the Pi (AlfredHomeHub
needed nothing), rebuilt with `docker compose up -d --build coinquest` from
docker/ (ARM64, built on the Pi), container up healthy with a clean log:
alembic ran 6131c9c1284a -> c3f8b21a7d40 against real rows and the table
rebuild survived them, which is the thing a test against a freshly-migrated
empty database cannot prove.

Verified against the real household database, not only the container's
health:

  - The rebuilt chore_instances carries every constraint it had except the
    one this session drops, and both partial unique indexes. Dumped and read,
    not assumed.
  - Row counts identical across all seven tables before and after (32 chore
    instances, 3 definitions, 2 weeks, the four ledgers empty), pragma
    integrity_check ok, pragma foreign_key_check empty, revision at head.
  - The endpoints round-tripped on a real instance and put it back: marking
    Thursday's chore missed with no PIN took /api/summary from £3.00 to
    £1.00 — the whole pot, on the real week — clearing it with a wrong PIN
    was refused 401, clearing it with the right one returned £3.00, and the
    row afterwards is byte-for-byte what it was (untouched, missed_at,
    miss_origin and authorised_by all null, rejection_count still 0).
  - One thing the household's own data says that no test did: with no
    on-demand bonus chore in this week's scheme, `recovery.options` is empty
    and `make_good` stays null even with a miss outstanding. The line shows
    nothing rather than telling him to do something he has not got — which
    is the rule, meeting the real scheme for the first time.


Session X (2026-08-27) - a downgrade that has never been run is not a
downgrade

1eb8e8b3e4ae's downgrade() had never executed once. Its batch_alter_table on
weeks dropped the three override columns without passing copy_from, so Alembic
reflected the live table to build the replacement and carried every CHECK
constraint's raw SQL across with it - including two that name overridden_by,
a column that same rebuild was dropping. SQLite refused the CREATE TABLE with
"no such column: overridden_by". Every sibling migration that rebuilds weeks
passes an explicit copy_from=_weeks_table() on both directions; this one had
it on upgrade() only.

Fixed to match them: copy_from=_weeks_table() and recreate="always". That
alone is not enough, because dropping a column does not drop the constraints
that mention it - the three CHECKs this revision added come off explicitly
first, by name, wrapped in op.f() so the metadata naming convention does not
prefix them a second time.

The second half was worse than the first. The downgrade drops the two weeks
immutability triggers at the top, as it must - the rebuild cannot happen with
them attached - and never put them back. The revision below it in the chain,
9040ce063e24, downgrades to nothing at all, so nothing else would have put
them back either. A database that had merely stepped back one revision would
have sat there with a settled week's figures freely editable and a closed week
freely deletable, and no error anywhere to say so. The whole append-only
design rests on those triggers; a downgrade that silently disarms them is a
worse outcome than one that refuses to run. They are restored in their
pre-revision shape - without the three lines naming the columns that no longer
exist - from a PRIOR_WEEKS_TRIGGERS block written out in full, the same way
every migration in this directory keeps its own copy rather than importing
one that can change meaning later.

Two new tests in tests/test_migration_against_real_data.py, seeded from
real-shaped data rather than an empty database: the existing seed gained a
settled week carrying its stored figures, seven more chore_instances, and a
second row in each ledger and in settlement_lines, all referencing it under
ON DELETE RESTRICT. Rebuilding a table means dropping it, and SQLite refuses
while real rows reference it - which is exactly why every earlier rebuild
passed at first deploy and failed in anger.

  - test_the_chain_walks_down_and_back_up_against_seeded_data walks every
    downgrade the chain can run, one revision at a time so a failure names the
    step, then comes back up and checks the version, integrity_check,
    foreign_key_check, every seeded row count, and the settled week's stored
    figures byte for byte.
  - test_the_downgrade_leaves_the_immutability_triggers_armed asserts the
    state the database is actually left in at the bottom: both triggers
    present, neither naming a dropped column, and - the part that matters -
    an UPDATE and a DELETE against the settled week both refused. Asserting a
    trigger exists proves only that something with the right name is in
    sqlite_master.

Both up legs go through app.db.run_migrations(), not a plain alembic CLI call.
The CLI never imports app.db and therefore never turns foreign-key enforcement
on, so it would pass on a database the app itself cannot migrate - the same
distinction Session V's incident turned on.

Proved to be regression tests rather than a claim of coverage. Reverting the
whole fix fails both new tests with the original "no such column:
overridden_by"; restoring it passes them. Reverting only the trigger
restoration, keeping the copy_from fix, fails the trigger test alone and
leaves the other three green - so each half of the fix is independently
covered. Full suite 526 passed.

The walk stops one revision above base rather than reaching it, and the test
says so in a named constant. 4e720209aaba's downgrade has exactly the same
defect, one revision lower - drops settled_base_pence with no copy_from, dies
on "no such column: settled_base_pence". Fixing a second historical migration
was not this session's remit. It is logged in docs/scribble.md with the note
that DOWNGRADE_FLOOR exists to be deleted when it is fixed.

One more thing surfaced and parked: e42da40283c5 adds authorised_by nullable
with a CHECK requiring it for any confirmed instance, and backfills nothing,
so it cannot migrate a database that already holds a confirmed chore instance.
82826e03b64d has the same shape for missed and miss_origin. Both have already
run against the live database without incident, so this is latent rather than
live - it bites a restore-then-migrate from a backup taken before those
revisions, which is precisely the drill still listed as undone at the top of
the scribble pad. It is also why the new seed's settled week holds claimed
instances rather than confirmed ones, said in a comment where a later reader
will need it.

Pushed and deployed to the household stack the same day. Unlike the last two
deploys this one carries no migration at all: the change is to an existing
revision's downgrade(), plus tests and docs, so head is still c3f8b21a7d40 and
alembic had nothing to run. The pre-migration backup was taken anyway
(coinquest-2026-08-27-1318.tar.gz) — it costs seconds, and "no migration this
time" is exactly the assumption that is wrong once. Pulled CoinQuest on the
Pi, rebuilt with `docker compose up -d --build coinquest` from
AlfredHomeHub/docker (ARM64, built on the Pi), container healthy in under 30
seconds with a clean log: alembic connected, found the database already at
head, and said nothing further.

Verified against the real household database: revision c3f8b21a7d40,
integrity_check ok, foreign_key_check empty, row counts unchanged across all
nine tables (4 definitions, 34 chore instances, 2 weeks, 1 scheme_settings,
the rest empty), and both weeks immutability triggers present — which is the
one thing this session could conceivably have disturbed, since restoring them
correctly on the way down is half of what it changed. /health, /api/summary
(£3.00, week of 2026-08-23, 2 days remaining) and the /parent deep link all
answer as they did before.

Follow-on to Session X (2026-08-27) - the same defect, one revision lower

Session X found 4e720209aaba's downgrade() carrying the identical bug it had
just fixed in 1eb8e8b3e4ae and parked it as out of remit. This clears it.

Same fix, and it needed more of it than the first one did, because this
revision does three things at once and the downgrade has to undo all three in
a single rebuild. copy_from=_weeks_table() with recreate="always"; the five
CHECKs naming settled_base_pence or settled_chore_pay_pence dropped by name
first, since dropping a column does not drop the constraints that mention it;
the column dropped; and - the part that differs from 1eb8e8b3e4ae - the rename
back to settled_basic_pence done *inside* the same batch rather than outside
it as upgrade() does, because the four restored CHECKs name that column and it
has to be called that by the time the replacement table is written.

The triggers again. This downgrade drops the two weeks immutability triggers
at the top and never put them back, and 82826e03b64d does not touch weeks at
all, so nothing below would have. Restored in their initial-schema shape,
naming settled_basic_pence.

Verified structurally rather than by absence of error: the table left behind
after the downgrade was dumped and compared element by element against what
82826e03b64d actually leaves when reached by upgrade. Identical - same
columns, same constraint names, same CHECK text - and the trigger SQL matches
the initial schema's.

One judgement recorded in the code and the scribble pad: the downgrade
restores ck_weeks_a_voided_week_pays_nothing, which is the constraint this
revision exists to relax. A voided week carrying rewards is legitimate now and
would fail it, so a downgrade over such a week aborts loudly rather than
zeroing somebody's rewards to make the DDL apply. That is c3f8b21a7d40's
precedent, not 9040ce063e24's - the difference being that leaving the relaxed
constraint in place would have meant another rebuild there, and here the
rebuild is already happening.

DOWNGRADE_FLOOR is gone from tests/test_migration_against_real_data.py, along
with the comment explaining why it existed. The walk now reaches base. That
last step - the initial schema's own downgrade, dropping every table while
seeded chore_instances still reference weeks under ON DELETE RESTRICT - is the
step with the most to say about the 2026-08-25 incident, and it destroys the
evidence it is standing on, so it got its own test rather than a line at the
end of the round trip:

  - test_the_chain_walks_down_and_back_up_against_seeded_data now walks to the
    first revision, asserts there that the settled week survives with its
    figures under the pre-rename column name, then comes back up and asserts
    the same figures under the post-rename one.
  - test_the_walk_reaches_base_with_real_rows_still_referencing takes the last
    step, confirms nothing but alembic_version remains, and re-applies the
    whole chain - a chain that cannot be re-applied after a full teardown has
    not been proved to walk.
  - test_the_downgrade_leaves_the_immutability_triggers_armed now checks the
    trigger names none of the four columns that do not exist at the bottom of
    the walk, settled_chore_pay_pence included, and still refuses an UPDATE
    and a DELETE against the settled week.

Proved as regression tests. Reverting the whole fix fails three of the five
with the original "no such column: settled_base_pence"; restoring it passes
them. Reverting only the trigger restoration, keeping the copy_from fix, fails
the trigger test alone and leaves the other four green - so each half is
independently covered, the same way Session X's was. Full suite green,
alembic check clean.

Pushed and deployed to the household stack the same day. Like the deploy
before it this one carries no migration: the change is to an existing
revision's downgrade(), plus tests and docs, so head is still c3f8b21a7d40 and
alembic found the database already there. Backup taken first anyway
(coinquest-2026-08-27-1346.tar.gz). Pulled on the Pi, rebuilt with
`docker compose up -d --build coinquest` from AlfredHomeHub/docker (ARM64,
built on the Pi), healthy inside 30 seconds.

Verified against the real household database: revision c3f8b21a7d40,
integrity_check ok, foreign_key_check empty, row counts unchanged across all
nine tables (4 definitions, 34 chore instances, 2 weeks, 1 scheme_settings,
the rest empty), both weeks immutability triggers present, and the trigger
still naming settled_chore_pay_pence - which is the check that matters here,
since this session's work was about restoring the *other* shape of that same
trigger on the way down. Head is unaffected, which is what it should be.
/health, /api/summary (£3.00, week of 2026-08-23, 2 days remaining) and the
/parent deep link all answer as before.

2026-09-01 — Session Z: the monthly savings match

Closed the gap app/routers/weeks.py had named since Session H: nothing
computed a match from the savings ledger. §8 of the pocket-money rules is
referenced from that gap but is not itself in this repository (checked
docs/, scribble.md, every migration and service - only one PRD line naming
the rule's shape, no numbers), so the arithmetic below came from Nick
directly rather than being inferred, and is recorded here since nowhere else
does:

  - The rate starts at a configured start rate (seeded 5%) and climbs one
    point a consecutive clean (no-withdrawal) month, up to a configured
    ceiling (seeded 10%). A first month with no withdrawal counts as clean,
    however partial.
  - A withdrawal resets that *same* month's own rate to the start rate, and
    that month is matched on the "dipped-to" low - the lowest balance after
    the withdrawal, not the whole month's. A reversal (a reopened week's
    payment, unwound) is not a withdrawal for this: it neither resets the
    ladder nor triggers the dipped-to-low treatment.
  - The match is the rate applied to whichever is lower, the month's low or
    a configured cap (seeded £100) - a rate ceiling on the matched portion,
    not a stop, so a balance past the cap keeps earning the same match.
  - The opening balance earns the match from month one, the same as a
    deposit would.

Followed settlement.py's shape exactly: `app/services/savings_match.py`
gains `propose()` (replays the savings ledger, writes nothing, safe to call
repeatedly) and `settle()` (checked against a fresh existence test and the
agreed figure, writes once, closes the month forever). There is no way to
ask propose() for an arbitrary month - only ever "the next one after the
last settled", which is what makes the ladder mean anything; a backlog of
unsettled months has to be worked through in order.

`SavingsMonthMatch` (savings_month_matches) is new: one row per settled
month, storing the low it matched on, whether a withdrawal happened, the
rate and cap in force, and the match - never a pointer back to
scheme_settings, for the same reason `weeks` stores its own settled figures
rather than rereading a chore definition. Its own CHECK reproduces the match
formula (`match_pence = (MIN(low, cap) * rate + 50) / 100`), so a row that
disagrees with its own arithmetic is refused by the database, not only by
whatever wrote it. Immutability triggers, unconditional this time (unlike
`weeks`, nothing about a settled month stays writable after payday). A
second settlement of the same month is refused both by the service (a fresh
query before writing) and by the table's own unique constraint on
period_start - reopening a settled month is deliberately out of scope, a
separate card, same as the prompt asked.

scheme_settings gained the three tunable figures (start rate, ceiling rate,
cap) alongside weekly_basic_pay_pence, reviewable through the same
`/api/settings` endpoint - now a whole-form POST (all four fields together,
no partial update) rather than the one-field write it used to be. Migration
7b10cc8d801f adds those columns plus savings_month_matches; head is now
7b10cc8d801f. `savings.record_match()` is the new ledger primitive (allows
zero, unlike record_deposit) and `app/routers/savings_match.py` is a new
router: GET /api/savings/match (settled months), GET
/api/savings/match/proposal (no PIN), POST /api/savings/match/settle (PIN).

tests/test_savings_match.py: the ladder climbing 5→10% across six clean
months and holding there on the seventh; a withdrawal resetting the same
month to 5% and paying on the dipped-to low, traced by hand against a
multi-withdrawal month; a reversal proved *not* to reset the ladder; the cap
example from the brief itself (£250 at 10%, capped at £100, pays £10 - as
its own pure function test) plus the same behaviour proved through a real
six-month climb where the balance is well past the cap by the time it
matters; a settled month's figures read unchanged after the cap and ceiling
are both raised; PIN-guarded settling; double-settlement refused; a stale
agreed figure refused; the match landing on the savings ledger as its own
entry. tests/test_scheme_settings.py extended for the three new fields and
the start-above-ceiling refusal. Full suite green (549), including
test_migration_against_real_data.py's walk up and down through the new
revision.

Not pushed or deployed. coinquest.db (the real household database, mounted
on the Pi via AlfredHomeHub/docker) sits at c3f8b21a7d40, one revision
behind - deliberately left alone this session rather than migrated in
passing, given this repo's own history with migrations run for the first
time against real data (Session V). Whoever runs the migration next should
back up first, the way every deploy since has. No frontend surface yet -
this card was the computation and the endpoints, not a screen; the proposal
and settle actions exist only via the API today.

2026-09-01 — Session Z follow-on: two screens for the match, and a backend
gap that surfacing it exposed

Nick confirmed the spec actually envisages three savings screens, not one:
an Oliver-facing Savings page, a Lifetime page (total earned and the
never-withdrawn counterfactual - separate, out of scope, coming later,
deliberately not folded into either of these two), and a parent panel to
review and settle the match. Built the first and third; the Lifetime page
stays unbuilt on purpose.

Building either screen surfaced a real gap in what Session Z shipped:
`propose()` refused outright with MonthNotOver until a month had actually
finished, which meant there was no way to answer "what would this pay right
now" for the month in progress - and both screens need exactly that
("the month's low so far", "what it will pay if nothing changes"). Fixed by
moving the refusal to where it actually belongs: `propose()` now computes
the same figures whether or not the month has ended, returning a new
`month_has_ended` flag; `settle()` is what refuses to close on one that
hasn't. Added `clean_months_in_a_row` too - the true streak behind the rate,
uncapped, so a screen can say "8 months clean" even once the rate itself
has been sitting at the ceiling for two of them - computed server-side
rather than reimplemented in TypeScript, the same judgement App.tsx's own
docstring already makes about not giving a screen a second copy of a rule
that has to agree with the one that actually settles.

tests/test_savings_match.py updated for the new split: propose() proved to
preview a live in-progress month without raising, settle() proved to refuse
one that hasn't ended, and clean_months_in_a_row asserted through the
ladder-climb and withdrawal-reset tests already there. 20 tests, still
green; full suite unaffected elsewhere.

Frontend, two new pieces:

  Parent panel (components/parent/SavingsMatch.tsx, wired into
  ParentView.tsx alongside SavingsPanel) - the rate in force, the month's
  low so far, and what it pays, with a Settle button that only appears once
  `month_has_ended` is true. Settle re-fetches the proposal the instant it
  is pressed and opens the PIN dialog on that fresh figure, never on
  whatever the panel last rendered - the identical readback-before-commit
  shape ThisWeek's own SettleButton already uses, down to fetching fresh
  and refusing to open the dialog if the month has stopped being the one
  waiting to settle in the meantime. The most recently settled month reads
  as a quiet line above it, the closest analogue this feature has to
  ClosedWeeks' read-only closed-week display - there is no "already
  settled, mid-transition" state to speak of, since propose() only ever
  names the next unsettled month and a parent watching this panel simply
  sees it roll forward to the following one the moment Settle succeeds.

  Oliver's Savings page (SavingsView.tsx, a third screen alongside child
  and parent, reached from a new "Savings" button in the footer - needs no
  PIN, so it sits beside Parent rather than behind it) - balance, current
  rate, clean months in a row, what next month pays if nothing changes,
  this month's low so far, and a progress bar toward the cap. Built from
  the same `.total` gradient card and `.figures` grid ThisWeek and Total.tsx
  already use, deliberately, rather than a second visual language - `Figure`
  itself is now exported from ThisWeek.tsx and shared by both new pieces,
  the same way PinDialog already crosses the child/parent boundary in
  App.tsx.

Fixed in passing: `updateSchemeSettings` on the frontend still only sent
`weekly_basic_pay_pence`, which the settings endpoint had required alone
until this session's earlier work made all four fields travel together on
every write - a real regression the Weekly Basic Pay editor would have hit
the next time anyone used it. Chores.tsx's call site now sends the other
three fields back exactly as it read them.

Verified by hand rather than only by test: started the real API against a
scratch database and the Vite dev server, seeded an opening balance, and
drove both screens with Playwright end to end - including forcing a month
into month_has_ended by dating the opening balance into August, settling it
through the actual Settle button and PIN dialog, and confirming the ladder
rolled forward to 6% on the new balance and the entry landed on the ledger
with the right reason string. No console errors from either screen. (One
pre-existing, unrelated console error was seen and left alone: POST
/api/week/open double-firing under React StrictMode's dev-mode double
effect invocation, on an endpoint neither this session nor Session Z
touched.) tsc -b, vite build, oxlint and the full Playwright e2e suite
(4/4) all clean; oxlint's four warnings are pre-existing, confirmed by
diffing against the same lint run on the prior commit.

Not pushed or deployed, same as the parent commit. coinquest.db is
untouched and still one revision behind, per the note above.
